
Gemini와의 대화
매일 정해진 시간에 자동으로 실행되어, 설정된 종목의 주가 데이터를 수집해 구글 시트에 기록하는 자동화 프로그램을 만들고 싶어.



나는 PRD를 작성하려고 해.

다음 항목들로 구성할 거야.



1. 프로젝트 개요 (이름, 한 줄 설명, 유형, 난이도)

2. 사용자 시나리오 (누가, 언제, 왜 쓰는지)

3. 핵심 기능 목록 (필수 기능, 선택 기능)

4. 기술 스택

5. 화면 구성

6. 상세 기능 명세

7. 디자인 가이드

8. 제약 사항



이 순서대로 하나씩 질문할 테니까, 나와 대화를 통해 하나씩 정해줘.

그리고 내가 다음 단계로 넘어가자 라고 하기 전까지는 해당 단계에 집중해줘.

주식 데이터 수집 자동화 프로그램 구축을 위한 PRD(제품 요구사항 정의서) 작성을 시작해 보겠습니다. 요청하신 대로 다른 단계로 넘어가지 않고, 오직 '1. 프로젝트 개요'에만 집중하여 대화를 진행하겠습니다.

1. 프로젝트 개요 작성하기
가장 먼저 이 프로젝트의 기본 뼈대를 잡아야 합니다. 아래 네 가지 항목에 대해 생각하고 계신 점을 말씀해 주세요.

프로젝트 이름: (예: StockCollector, 주린이의 자동 시트, MyStockBot 등)

한 줄 설명: (예: 매일 장 마감 후 관심 종목의 종가를 구글 시트에 자동으로 기록하는 프로그램)

유형: (예: 웹 앱, 데스크톱 프로그램, 백엔드 스크립트 등)

난이도: (본인의 현재 개발 역량이나 구현하고 싶은 복잡도에 따라 상/중/하로 설정)

위 항목들에 대해 편하게 의견을 주시면, 객관적으로 분석하여 다듬어 드리겠습니다.

추가로, 이 단계를 구체화하는 데 도움이 될 만한 질문을 미리 세 가지 드립니다. 답변 시 함께 고려해 주세요.

수집하고 싶은 주식 시장이 어디인가요? (국내 주식(KOSPI/KOSDAQ), 해외 주식(미국 등), 또는 둘 다인지)

이 프로그램을 완전히 백그라운드(서버)에서 알아서 돌릴 생각인가요, 아니면 본인 컴퓨터를 켜두었을 때 실행되는 방식을 원하시나요? (유형과 난이도 결정에 중요합니다.)

구글 시트 외에 다른 UI(사용자 화면)가 별도로 필요하신가요?

프로젝트 이름은 MyStockBot으로 정할께. 유형은 웹 앱으로 진행해줘

보내주신 의견을 바탕으로 '1. 프로젝트 개요'의 초안을 작성하고, 이에 대한 객관적인 분석과 피드백을 드리겠습니다.

1. 프로젝트 개요 (초안)
프로젝트 이름: MyStockBot

한 줄 설명: 매일 정해진 시간에 관심 종목의 주가 데이터를 자동으로 수집하여 구글 시트에 기록하는 웹 애플리케이션

유형: 웹 애플리케이션 (Web Application)

난이도: 중 ~ 상 (조정 필요)

짚고 넘어가야 할 분석 및 피드백 (오류 잡아내기)
사용자께서 유형을 '웹 앱'으로 선택해 주셨습니다. 하지만 개발 목적이 "매일 정해진 시간에 자동으로 실행되는 주가 데이터 수집"이라면, 일반적인 '웹 앱' 형태는 비효율적이거나 잘못된 방향일 수 있습니다. 이 부분을 바로잡아 드릴게요.

'웹 앱'과 '자동화 스크립트'의 차이

웹 앱은 사용자가 브라우저로 접속해서 화면을 보고 버튼을 누르는 등 '상호작용'을 하는 프로그램입니다.

반면, 특정 시간에 자동으로 돌아가는 기능은 화면이 없는 '백엔드 스크립트(또는 배치 프로그램)'의 영역입니다.

왜 '웹 앱'으로 하면 난이도가 올라갈까요?

단순히 데이터만 모아서 시트에 넣으면 되는데, 웹 앱으로 만들면 사용자가 로그인하는 화면, 종목을 등록하는 화면, 웹 서버 호스팅 비용 등 신경 써야 할 과제가 너무 많아집니다. 난이도가 '하'에서 '중~상'으로 급격히 올라갑니다.

가장 추천하는 올바른 방향

구글 앱스 스크립트(Google Apps Script, GAS)를 사용하면 별도의 웹 앱 구축 없이, 구글 시트 자체에 내장된 코드로 매일 특정 시간에 주가를 가져오도록 가장 쉽고 완벽하게 구현할 수 있습니다.

💡 결론 및 제안: > 만약 굳이 웹 화면(대시보드 등)이 필요한 게 아니라면, 유형을 **'구글 시트 내장형 자동화 스크립트'**로 변경하는 것을 강력히 권장합니다. 이렇게 하면 난이도는 **'하'**로 내려가며 목적에 가장 부합합니다. 웹 화면이 반드시 필요한 이유가 있다면 그대로 진행하겠습니다.

다음 단계로 넘어가기 전, 개요를 확실히 확정하기 위해 아래 질문에 답변해 주세요.

피드백을 반영하여 유형을 '자동화 스크립트'로 변경할까요, 아니면 반드시 웹 화면이 필요한 이유가 있어서 '웹 앱'을 유지할까요?

프로젝트의 '한 줄 설명'에 포함될 주식 시장(국내 주식 vs 해외 주식)은 무엇인가요?

위 내용이 수정된다면, 난이도를 어떻게 정의하고 싶으신가요? (본인의 개발 숙련도를 고려해 주세요.)

너의 추천대로할꼐

피드백을 적극적으로 수용해 주셔서 감사합니다. 제안해 드린 방향이 프로젝트를 가장 빠르고 안정적으로 완성할 수 있는 방법입니다.

의견을 바탕으로 '1. 프로젝트 개요' 단계를 완성하여 정리해 드립니다.

1. 프로젝트 개요 (확정)
프로젝트 이름: MyStockBot

한 줄 설명: 구글 앱스 스크립트(GAS)를 활용하여 매일 정해진 시간에 관심 종목의 주가 데이터를 자동으로 수집하고 구글 시트에 기록하는 내장형 자동화 프로그램

유형: 구글 시트 내장형 자동화 스크립트 (Google Apps Script)

난이도: 하 (웹 서버 구축 및 호스팅이 필요 없어 입문자도 쉽게 구현 가능)

이로써 첫 번째 항목인 '1. 프로젝트 개요'가 성공적으로 마무리가 되었습니다.

이제 다음 단계인 '2. 사용자 시나리오'로 넘어가도 괜찮으실까요? 넘어가기 전에 추가로 생각해보실 만한 질문 3가지를 드립니다.

'다음 단계로 넘어가자'라고 말씀해 주시면 바로 2단계를 시작할까요?

이 프로그램이 주가 데이터를 수집할 때, 혹시 '국내 주식' 위주로 수집할 계획이신가요? (2단계 시나리오에 반영하기 위함입니다.)

매일 '몇 시'쯤 데이터가 시트에 자동으로 기록되기를 원하시나요? (예: 장 마감 직후인 오후 4시, 혹은 출근 전 오전 7시 등)

2단계로 넘어가줘

좋습니다. '1. 프로젝트 개요'를 확정했으니, 이어서 '2. 사용자 시나리오' 단계로 넘어가겠습니다.

사용자 시나리오는 이 프로그램을 누가, 언제, 왜(어떤 상황에서) 사용하는지 구체적으로 정의하여, 앞으로 만들 기능들의 타당성을 검증하는 단계입니다.

2. 사용자 시나리오 작성하기
자동화 프로그램의 특성상 사용자가 직접 조작하는 순간과 시스템이 스스로 돌아가는 순간이 나뉩니다. 제가 가상의 시나리오 예시를 구성해 보았습니다. 읽어보시고 수정하거나 추가하고 싶은 부분이 있는지 말씀해 주세요.

[가상 시나리오 예시]
누가: 주식 포트폴리오를 관리하고 싶지만, 매일 주가를 일일이 확인하고 엑셀에 타이핑하기 귀찮은 주식 투자자 (본인).

언제:

최초 1회: 구글 시트에 내가 관심 있는 종목 코드(티커)를 등록할 때.

매일 특정 시간 (예: 장 마감 이후): 프로그램이 스스로 작동하여 데이터를 기록할 때.

수시로: 사용자가 누적된 주가 데이터와 자산 변화 그래프를 확인하고 싶을 때.

왜: 매일 수작업으로 주가를 기록하는 번거로움을 없애고, 장기적인 주가 트렌드를 자동으로 축적하여 투자 판단에 활용하기 위해.

짚고 넘어가야 할 분석 및 피드백
자동화 프로그램 시나리오에서 흔히 하는 실수는 "사용자가 아무것도 안 해도 된다"고 생각하는 것입니다. 하지만 최초에 종목을 추가하거나, 필요 없는 종목을 삭제하는 등 '종목 관리'를 하는 시점이 시나리오에 반드시 포함되어야 합니다. 그래야 6단계 기능 명세에서 관리 기능이 누락되지 않습니다.

위 예시 시나리오에 동의하시는지, 혹은 수정하고 싶은 구체적인 상황이 있는지 알려주세요.

더불어 다음 단계로 나아가기 위한 3가지 질문입니다.

주가를 기록하는 시점은 하루 중 언제로 설정하는 것이 가장 좋을까요? (예: 장이 마감된 직후인 오후 4시, 혹은 하루가 끝나는 자정 등)

이 시나리오에서 수집된 데이터를 바탕으로 향후 '수익률 계산'이나 '자산 시각화 차트'까지 볼 계획이 있으신가요?

위 시나리오가 마음에 드신다면, 다음 단계(3. 핵심 기능 목록)로 바로 넘어갈까요?

3단계로 넘어가줘

'2. 사용자 시나리오'를 바탕으로, 프로젝트의 뼈대가 될 '3. 핵심 기능 목록' 단계로 넘어가겠습니다.

이번 단계에서는 프로그램이 반드시 갖춰야 할 '필수 기능'과, 있으면 좋지만 나중에 구현해도 되는 '선택 기능'을 명확히 분류해야 개발이 산으로 가지 않습니다.

3. 핵심 기능 목록 (초안)
구글 앱스 스크립트(GAS) 환경에 맞춰 구성한 기능 목록입니다. 객관적으로 분석하여 분류했습니다.

1) 필수 기능 (MVP - 이것 없으면 프로그램의 의미가 없음)
종목 설정 및 관리: 구글 시트의 특정 영역에 사용자가 주식 종목 코드(예: 삼성전자 005930, 애플 AAPL)를 입력하면 이를 인식하는 기능

주가 데이터 수집 (크롤링/API): 설정된 종목의 당일 종가(Price) 데이터를 외부에서 가져오는 기능

시트 자동 기록 (Appender): 가져온 주가 데이터를 지정된 시트에 날짜와 함께 누적하여 기록하는 기능

시간 기반 트리거 (Timer): 매일 정해진 시간(예: 장 마감 후)에 코드가 알아서 실행되도록 구글 트리거를 설정하는 기능

2) 선택 기능 (추후 확장 가능한 보너스 기능)
알림 기능: 데이터 수집이 성공 완료되었거나, 오류가 발생했을 때 카카오톡이나 이메일(Gmail)로 알림을 보내주는 기능

오류 예외 처리: 주말/공휴일 등 장이 열리지 않는 날에는 실행을 건너뛰거나, 잘못된 종목 코드가 있을 때 에러를 뿜지 않고 패스하는 기능

짚고 넘어가야 할 분석 및 피드백 (오류 잡아내기)
많은 입문자분들이 필수 기능에 "국내 주식과 해외 주식 주가를 실시간으로 가져오기"를 넣고 싶어 합니다. 하지만 여기서 두 가지 치명적인 문제가 발생할 수 있어 바로잡아 드립니다.

국내/해외 주식 데이터 소스의 이원화

구글 시트에는 =GOOGLEFINANCE()라는 강력한 내장 함수가 있습니다. 미국 주식은 이 함수로 실시간에 가깝게 가져올 수 있지만, 국내 주식은 20분 지연되거나 간혹 데이터가 누락되는 고질적인 문제가 있습니다.

따라서 국내 주식을 정확히 수집하려면 네이버 금융 등에서 크롤링(데이터 추출)을 해야 할 수 있습니다.

트리거 실행 시간의 한계

구글 앱스 스크립트의 시간 트리거는 "오후 4시 정각"처럼 정밀하게 세팅할 수 없고, "오후 4시~5시 사이" 같은 시간 범위(Window)로만 실행 지정이 가능합니다. 초 단위의 정밀한 스케줄링은 불가능하다는 제약 사항을 인지해야 합니다.

💡 제안: > 처음에는 복잡한 크롤링 코드를 짜기보다, 구글 시트 내장 함수(=GOOGLEFINANCE)를 활용해 데이터를 가져오는 방식을 '필수 기능'으로 잡고, 이것이 실패할 경우 네이버 금융을 긁어오는 방식을 '선택 기능'으로 돌리는 것이 안전합니다.

기능 목록을 확정하고 다음 단계로 가기 위해 아래 3가지 질문에 의견을 주세요.

수집할 데이터 종류는 '종가(현재가)' 하나면 충분한가요, 아니면 거래량, 시가, 등락률 등도 함께 수집하고 싶으신가요?

수집 완료/실패 시 이메일(Gmail) 등으로 알림을 받는 '선택 기능'을 포함할까요?

위 기능 목록 수정에 동의하신다면, 다음 단계(4. 기술 스택)로 넘어갈까요?

알림 기능: 데이터 수집이 성공 완료되었거나, 오류가 발생했을 때 카카오톡이나 이메일(Gmail)로 알림을 보내주는 기능

오류 예외 처리: 주말/공휴일 등 장이 열리지 않는 날에는 실행을 건너뛰거나, 잘못된 종목 코드가 있을 때 에러를 뿜지 않고 패스하는 기능



둘다 추가해주고 다음 단계로 넘어가줘. 다중 종목 처리 기능도 추가해줘 

선택 기능으로 제안해 드린 '알림 기능'과 '오류 예외 처리'를 모두 필수 기능으로 격상하고, '다중 종목 처리 기능'까지 반영하여 '3. 핵심 기능 목록'을 최종 확정했습니다.

이어서 '4. 기술 스택' 단계로 넘어가겠습니다.

3. 핵심 기능 목록 (최종 확정)
필수 기능:

다중 종목 설정 및 관리: 구글 시트에 여러 개의 종목 코드(예: 삼성전자, 애플, 테슬라 등)를 리스트 형태로 입력하면 프로그램이 이를 한 번에 인식하는 기능

주가 데이터 수집: 설정된 다중 종목의 당일 종가 데이터를 자동으로 가져오는 기능

시트 데이터 누적 기록: 매일 새로운 행을 추가하여 [날짜 | 종목명 | 종목코드 | 종가] 형태의 데이터를 누적 저장하는 기능

시간 기반 트리거: 매일 지정된 시간 범위에 코드가 자동 실행되도록 설정하는 기능

오류 예외 처리: * 주말 및 공휴일 등 장이 열리지 않는 날에는 데이터 수집을 건너뛰는 기능

잘못된 종목 코드가 입력되어도 프로그램이 멈추지 않고 다음 종목으로 넘어가는 예외 처리

알림 기능: 데이터 수집이 성공적으로 완료되었거나, 실행 중 오류가 발생했을 때 이메일(Gmail) 또는 카카오톡으로 알림 메시지를 전송하는 기능

4. 기술 스택 정의하기
우리가 1단계에서 유형을 '구글 시트 내장형 자동화 스크립트'로 정했기 때문에, 기술 스택은 매우 직관적이고 가볍게 구성됩니다. 복잡한 서버나 데이터베이스(DB)를 구축할 필요가 없어 비용이 전혀 들지 않는다는 강력한 장점이 있습니다.

개발 언어 및 환경: Google Apps Script (GAS)

구글이 제공하는 자바스크립트(JavaScript) 기반의 클라우드 스크립팅 언어입니다. 브라우저에서 바로 코딩할 수 있습니다.

데이터베이스 (DB): Google Sheets (구글 시트)

별도의 SQL이나 NoSQL DB를 쓰지 않고, 구글 시트의 셀 자체를 데이터를 저장하는 DB로 활용합니다.

외부 연동 및 알림 API:

GmailApp (GAS 내장 서비스): 구글 계정과 연동하여 가장 쉽고 안정적으로 이메일 알림을 보낼 수 있습니다.

KakaoTalk Messaging API (선택 서비스): 카카오톡 알림을 원할 경우, 카카오 디벨로퍼스 등록을 통해 외부 HTTP 요청(UrlFetchApp)으로 연동합니다.

짚고 넘어가야 할 분석 및 피드백 (잘못된 방향 바로잡기)
알림 기능에서 '카카오톡'과 '이메일(Gmail)' 두 가지를 언급하셨습니다. 개발 효율성 관점에서 객관적으로 분석해 드리겠습니다.

카카오톡 알림의 제약과 난이도 (틀린 방향 교정)

많은 분들이 카카오톡 알림을 쉽게 생각하지만, 카카오 API는 보안 정책상 토큰 갱신(Refresh Token) 프로세스가 까다롭고, '나에게 보내기' 기능도 일정 주기로 인증을 다시 해주어야 하는 번거로움이 있습니다. 코드가 복잡해져서 난이도가 상승합니다.

Gmail 알림의 압도적인 편리함

반면 구글 앱스 스크립트 내장 기능인 MailApp.sendEmail()을 사용하면, 단 한 줄의 코드로 본인의 Gmail 계정을 통해 스마트폰으로 즉시 푸시 알림을 받을 수 있습니다.

💡 결론 및 제안: > 기술 스택을 확정할 때, 우선 1순위로 구글 내장 이메일(Gmail) 알림을 기본 스택으로 지정하여 빠르게 완성하는 것을 강력히 추천합니다. 카카오톡은 이메일 알림이 성공적으로 잘 돌아갈 때, 추후 고도화 단계에서 추가하는 것이 개발 피로도를 줄이는 길입니다.

기술 스택을 확정하고 다음 단계인 '5. 화면 구성'으로 가기 위해 3가지 질문을 드립니다.

알림 수단을 우선 구현이 쉽고 직관적인 '이메일(Gmail)'로 집중하여 기술 스택을 확정해도 괜찮을까요?

주가 데이터를 가져올 때 구글 시트 내장 함수를 코드 내에서 호출하는 방식(UrlFetchApp 또는 Formula 활용)을 사용할 예정인데, 자바스크립트(JS) 문법에 어느 정도 익숙하신가요? (6단계 기능 명세의 상세 수준을 결정하기 위함입니다.)

기술 스택이 이대로 확정된다면, 다음 단계(5. 화면 구성)로 바로 넘어갈까요?

주식 시세 데이터 수집은 어떻게 사용할거야?

주식 시세 데이터를 어떤 방식으로 수집(크롤링 또는 API 연동)할 것인가는 이 프로그램의 안정성과 구현 난이도를 결정하는 매우 중요한 기술 스택 요소입니다.

구글 앱스 스크립트(GAS) 환경에서 주가 데이터를 가져오는 방법은 크게 두 가지가 있으며, 제안해 드리는 최종 선택안과 그 이유를 객관적으로 분석해 드리겠습니다.

1. 주식 데이터 수집 방식 후보 분석
① 구글 시트 내장 함수 활용 방식 (GOOGLEFINANCE)
구글 시트가 자체적으로 제공하는 =GOOGLEFINANCE("종목코드", "price") 함수를 코드(GAS) 내에서 호출하거나 셀에 입력하여 데이터를 가져오는 방식입니다.

장점: 별도의 크롤링 코드가 필요 없고, 서버 차단의 위험이 전혀 없으며, 미국 등 해외 주식은 실시간에 가깝게 완벽하게 가져옵니다.

틀린 부분 바로잡기 (단점): 국내 주식(KOSPI/KOSDAQ)의 경우, 구글 파이낸스 데이터에 20분 지연이 발생하거나 특정 종목의 당일 종가가 정시(예: 오후 4시)에 바로 업데이트되지 않고 누락되는 현상이 종종 발생합니다.

② 웹 크롤링 방식 (UrlFetchApp + 네이버 금융)
GAS의 내장 기능인 UrlFetchApp을 사용하여 네이버 금융 등의 주식 정보 페이지 HTML을 원격으로 긁어온 뒤, 정규표현식이나 문자열 파싱으로 당일 종가를 추출하는 방식입니다.

장점: 국내 주식의 경우, 장 마감(오후 3시 30분) 이후 정확한 당일 최종 종가를 가장 확실하게 가져올 수 있습니다.

단점: 네이버 금융의 웹 페이지 구조(HTML 태그 등)가 변경되면 코드가 작동하지 않아 주기적으로 코드를 수정해야 하는 유지보수 문제가 있습니다.

💡 최종 추천 및 사용 방식 제안
프로젝트의 안정성과 정확성을 모두 잡기 위해, 두 가지 방식을 조합한 ‘하이브리드(Failsafe) 방식’을 제안합니다.

기본 작동: 수집하려는 종목이 해외 주식이거나 실시간 지연이 상관없는 종목이라면 구현이 가장 간단한 GOOGLEFINANCE 함수를 코드로 호출하여 수집합니다.

국내 주식 대응: 정확한 당일 장 마감 종가가 필요한 국내 주식은 네이버 금융의 모바일 페이지(PC 페이지보다 구조가 단순하여 크롤링이 쉬움)를 UrlFetchApp으로 긁어와 종가를 추출하는 방식을 사용하겠습니다.

이렇게 설계하면 국내/해외 주식을 모두 안정적으로 처리할 수 있으며, 3단계에서 정의한 '오류 예외 처리'와도 잘 맞물리게 됩니다.

'4. 기술 스택' 중 데이터 수집 방식에 대해 검토해 보았습니다. 이 제안대로 수집 방식을 확정하고 다음 단계인 '5. 화면 구성'으로 넘어가도 괜찮으실까요?

다음 단계로 가기 전, 예상되는 추가 질문 3가지입니다.

투자하고 계신(또는 수집할) 종목은 주로 국내 주식인가요, 미국 등 해외 주식인가요?

크롤링 방식을 사용할 때 발생할 수 있는 '웹사이트 구조 변경으로 인한 코드 수정' 가능성을 감수하고 진행하시겠습니까?

이 내용이 정리되었다면, 다음 단계(5. 화면 구성)로 바로 넘어갈까요?

주로 국내 주식일 예정이야. 실행환경은 어떻게 될까?

국내 주식 위주로 수집하신다면, 앞서 제안해 드린 '네이버 금융 크롤링(UrlFetchApp)' 방식이 메인이 될 것입니다. 장 마감 후 가장 정확한 국내 주가 데이터를 긁어올 수 있습니다.

사용자께서 질문하신 '실행 환경'에 대해 명확히 짚고 넘어가겠습니다. 이 부분은 많은 분들이 오해하시는 부분이라 객관적으로 바로잡아 드릴게요.

4. 실행 환경 (Infrastructure) 분석
사용자가 구축할 MyStockBot은 본인의 컴퓨터나 스마트폰에서 돌아가는 것이 아닙니다.

실행 주체: 구글 클라우드 서버 (Google Cloud Infrastructure)

상세 실행 방식: 구글 앱스 스크립트(GAS)는 구글이 자사 클라우드에서 코드를 무료로 실행해 주는 환경을 제공합니다. 따라서 본인 컴퓨터를 꺼두어도, 스마트폰이 오프라인 상태여도 구글의 서버가 알아서 매일 정해진 시간에 잠에서 깨어나 코드를 실행합니다.

비용: 완전 무료 (하루에 외부 웹사이트를 긁어오거나 이메일을 보낼 수 있는 무료 할당량이 제공되는데, 개인 투자자 수준의 다중 종목 수집으로는 이 할당량의 1%도 쓰지 않습니다.)

⚠️ 흔히 하는 착각 교정:
"컴퓨터 켜놔야 하나요?", "프로그램을 상시 켜두어야 하나요?" -> 아닙니다. 구글 서버가 365일 교대로 알아서 돌려주기 때문에, 사용자는 구글 시트 창을 닫아두고 일상생활을 하시면 됩니다.

이로써 '4. 기술 스택 및 실행 환경'까지 완벽하게 정리가 되었습니다.

이제 다음 단계인 '5. 화면 구성'으로 넘어갈 차례입니다. 구글 시트 자체를 화면(UI)으로 사용할 예정이므로, 시트를 어떻게 쪼개서 쓸지 구상해야 합니다.

다음 단계로 넘어가기 전, 예상 질문 3가지입니다.

실행 환경(구글 서버에서 무료 자동 실행)에 대해 완벽히 이해하셨다면, 다음 단계(5. 화면 구성)로 넘어갈까요?

구글 시트 하나에 '종목 설정 화면'과 '데이터가 쌓이는 화면'을 탭(시트)으로 분리하고 싶으신가요, 아니면 한 화면에 다 모으고 싶으신가요?

알림 수단은 카카오톡 대신 구현이 확실한 '이메일(Gmail)'로 최종 확정해도 괜찮으실까요?

github action을 사용한다면?

만약 구글 앱스 스크립트(GAS) 대신 GitHub Actions(깃허브 액션스)를 실행 환경으로 사용한다면, 프로젝트의 구조와 기술 스택이 완전히 바뀌게 됩니다.

이 방식 역시 개발자들이 자동화 스크립트를 돌릴 때 정말 많이 쓰는 훌륭한 방법입니다. GAS 방식과 무엇이 다르고 어떤 장단점이 있는지 객관적으로 분석해 드릴게요.

4. 기술 스택 및 실행 환경 (GitHub Actions 대안 분석)
실행 주체: GitHub 인프라 서버 (Runner)

작동 방식: 1. 깃허브 저장소(Repository)에 주가를 긁어오는 코드(Python 또는 Node.js 등)를 작성해 둡니다.
2. 깃허브 액션스의 스케줄러(Cron Trigger)가 매일 정해진 시간(예: 오후 4시)에 가상 컴퓨터(서버)를 실행합니다.
3. 이 가상 컴퓨터가 코드를 실행하여 네이버 금융에서 주가를 크롤링합니다.
4. 크롤링한 데이터를 구글 시트 API(Google Sheets API)를 통해 내 구글 시트에 원격으로 입력합니다.

비용: 무료 (퍼블릭 저장소 기준 월 2,000분 무료 분량을 주는데, 하루 1~2분이면 충분하므로 평생 무료 가깝게 쓸 수 있습니다.)

짚고 넘어가야 할 분석 및 피드백 (GAS 대비 장단점 교정)
GitHub Actions는 매우 강력하지만, 현재 만들고자 하는 프로젝트 기준으로는 구현 난이도가 몇 배로 상승합니다. 그 이유는 다음과 같습니다.

보안 인증(OAuth2/Service Account)의 장벽 (틀린 부분 바로잡기)

GAS는 구글 서비스 안에서 돌기 때문에 내 구글 시트에 접근할 때 별도의 인증이 필요 없습니다.

반면, GitHub Actions는 '외부 서버'이기 때문에 구글 시트에 데이터를 쓰려면 구글 클라우드 콘솔에서 프로젝트를 만들고, API를 활성화하고, 서비스 계정 키(JSON)를 발급받아 깃허브 비밀키(Secrets)에 등록해야 합니다. 입문자분들이 여기서 가장 많이 포기합니다.

시간 트리거의 정밀도 문제

GitHub Actions의 Cron 트리거는 정밀하게 분 단위 설정을 할 수 있다는 장점이 있지만, 전 세계 수많은 개발자들이 동시에 무료 서버를 쓰기 때문에 스케줄링된 시간보다 최소 10분에서 최대 1~2시간까지 지연 실행되는 일이 빈번합니다. 장 마감 직후 정시 실행을 보장하기 힘듭니다.

💡 최종 권장안:

코드 관리, 파이썬(Python) 사용 숙련도, 향후 포트폴리오 활용이 목적이라면 GitHub Actions가 좋은 선택입니다.

하지만 **"가장 빠르고 삽질 없이 구글 시트에 데이터를 넣는 자동화"**가 목적이라면 여전히 **구글 앱스 스크립트(GAS)**가 압도적으로 유리합니다.

실행 환경을 1) 구글 앱스 스크립트(GAS)와 2) GitHub Actions 중 어느 쪽으로 최종 결정하시겠습니까? 결정을 내리시면 다음 단계인 '5. 화면 구성'으로 넘어가겠습니다.

다음 단계로 가기 전, 예상 질문 3가지입니다.

복잡한 API 연동 과정을 감수하더라도 파이썬 등을 쓸 수 있는 GitHub Actions 방식을 원하시나요?

실행 환경을 최종 확정하고 다음 단계(5. 화면 구성)로 바로 넘어갈까요?

화면 구성 단계로 가기 전, 구글 시트에서 한 셀에 여러 종목을 쉼표로 구분할지, 아니면 행(Row)별로 하나씩 적을지 구상해 두신 게 있나요?

github actionㅇ르ㅗ 진행할께

실행 환경을 GitHub Actions(깃허브 액션스)로 최종 결정하셨군요!

파이썬(Python) 같은 대중적인 언어를 사용할 수 있고, 코드 버전 관리가 용이하다는 점에서 개발자 지망생이나 현업 개발자들이 아주 선호하는 강력한 방식입니다.

실행 환경이 바뀜에 따라 '4. 기술 스택'을 다음과 같이 업데이트하고, 다음 단계인 '5. 화면 구성'으로 넘어가겠습니다.

4. 기술 스택 및 실행 환경 (최종 업데이트)
실행 환경: GitHub Actions (무료 클라우드 러너)

개발 언어: Python 3.x (웹 크롤링과 데이터 처리에 가장 최적화된 언어)

핵심 라이브러리:

requests 또는 BeautifulSoup4: 네이버 금융 국내 주가 크롤링용

gspread: 구글 시트 API를 파이썬으로 쉽게 제어하기 위한 라이브러리

데이터베이스: Google Sheets (Google Sheets API v4 연동)

보안 관리: GitHub Secrets (구글 API 인증 키 등 민감 정보 암호화 저장)

5. 화면 구성 정의하기
우리는 외부(GitHub Actions)에서 구글 시트로 데이터를 밀어 넣는 방식을 취하므로, 구글 시트의 '탭(시트) 구성'이 곧 이 프로그램의 화면(UI) 구성이 됩니다.

사용자가 다루기 가장 직관적인 2개의 시트(Tab) 분리 구조를 제안합니다.

시트 1: Dashboard (종목 설정 및 대시보드 화면)
사용자가 직접 텍스트를 입력하고 확인하는 유일한 화면입니다.

A열 (종목코드): 수집하고 싶은 국내 주식 종목코드 입력 (예: 005930, 035720)

B열 (종목명): 사용자가 알아보기 쉽게 적는 종목 이름 (예: 삼성전자, 카카오)

C열 (상태/최근수집일): 수집이 잘 되고 있는지 프로그램이 역으로 적어주는 칸 (선택)

시트 2: StockData (데이터 누적 저장소)
GitHub Actions가 매일 자동으로 데이터를 아래로 쌓아나가는 화면입니다. 사용자는 이 시트를 직접 수정하지 않습니다.

구조 (Header): [ 날짜 | 종목코드 | 종목명 | 당일종가 ]

예시 데이터:

날짜	종목코드	종목명	당일종가
2026-06-01	005930	삼성전자	75,000
2026-06-01	035720	카카오	48,000
2026-06-02	005930	삼성전자	75,500
짚고 넘어가야 할 분석 및 피드백 (화면 구성 시 주의점)
텍스트 형식(Format) 지정의 중요성 (오류 예외 처리 연동)

파이썬의 gspread 라이브러리로 구글 시트의 종목코드를 읽어올 때, 종목코드가 005930처럼 앞자리에 0이 있으면 구글 시트가 이를 '숫자'로 인식해 5930으로 변환해 버리는 흔한 오류가 있습니다.

이를 방지하기 위해 구글 시트의 종목코드 입력 열은 반드시 '텍스트 형식'으로 지정해야 한다는 제약 조건을 화면 구성 단계에서 염두에 두어야 합니다.

누적 데이터의 정렬 방식

매일 데이터가 쌓일 때, 기존 데이터를 뒤로 밀고 가장 최신 날짜를 위(1행)로 올릴지, 아니면 엑셀처럼 맨 아래(Last Row)에 계속 추가할지 정해야 합니다. 일반적으로 자동화 프로그램은 맨 아래에 이어 붙이는 형식이 코딩하기 훨씬 단순하고 에러가 적습니다.

💡 결론: 맨 아래에 순차적으로 누적하는 구조를 채택하되, 종목코드가 깨지지 않도록 시트 서식을 서포트하는 설계를 반영하겠습니다.

화면 구성을 확정하고 다음 단계인 '6. 상세 기능 명세'로 넘어가기 전, 생각해볼 3가지 질문입니다.

제안해 드린 Dashboard(설정)와 StockData(누적) 2개 탭 구조로 화면 구성을 확정해도 괜찮을까요?

GitHub Actions를 사용하므로, 데이터 수집 결과 이메일 알림은 구글(GAS) 대신 파이썬 소스코드 내에서 이메일을 발송하는 방식으로 처리하게 되는데 인지하고 계시나요?

화면 구성이 만족스러우시다면, 다음 단계(6. 상세 기능 명세)로 바로 넘어갈까요?

1. 스트링 타입인지 잘 체크할 수 있게 진행하고, 2. 에러가 적은쪽으로 진행할꼐

제안해 드린 화면 구성(2개 탭 구조)과 데이터 누적 방식(맨 아래에 순차적 추가)을 채택해 주셨습니다.

특히 종목 코드를 읽어올 때 1) 스트링(문자열) 타입을 엄격히 체크하고, 2) 에러 발생 가능성을 최소화하는 방향으로 코드를 설계하겠다는 점을 화면 구성 및 향후 기능 명세에 적극 반영하겠습니다.

파이썬의 gspread 라이브러리를 쓸 때 get_all_values() 대신 데이터 타입을 보존하거나 명시적으로 문자열로 변환하는 로직을 뒷단에 심으면 말씀하신 에러를 완벽히 차단할 수 있습니다.

이로써 '5. 화면 구성' 단계를 마무리하고, 프로젝트의 가장 핵심이자 구체적인 설계도인 '6. 상세 기능 명세' 단계로 넘어가겠습니다.

6. 상세 기능 명세 (Step-by-Step)
GitHub Actions 환경과 파이썬 프로그램이 실제로 어떤 순서로 움직이는지 로직을 아주 상세하게 쪼갠 명세입니다. 에러를 줄이기 위해 예외 처리 단계를 촘촘하게 배치했습니다.

[기능 1] GitHub Actions 스케줄러 작동
트리거 조건: 월~금요일 한국 시간(KST) 오후 4시 (Cron 표현식 기준 수정을 고려하여 설정)

상세: 깃허브 서버가 켜지면 저장소의 파이썬 코드를 실행하고, GitHub Secrets에 숨겨둔 구글 API 인증 키(credentials.json)를 불러와 가상 환경에 세팅합니다.

[기능 2] 구글 시트 종목 코드 로드 및 검증 (에러 방지 핵심)
상세: Dashboard 시트의 A열(종목코드) 데이터를 읽어옵니다.

예외 처리 및 타입 체크:

숫자로 읽혀서 5930이 된 경우, 앞자리에 0을 채워 무조건 6자리 문자열(String) 형태(005930)로 포맷팅을 강제합니다. (파이썬의 .zfill(6) 함수 사용)

빈 행(Empty Row)이나 주식 코드가 아닌 잘못된 텍스트가 들어가 있다면 데이터 수집 대상에서 제외하는 필터링 로직을 수행합니다.

[기능 3] 네이버 금융 국내 주가 크롤링
상세: 수집 대상 종목 코드를 순회하며 네이버 금융 모바일 페이지(https://m.stock.naver.com/item/{종목코드}/total)에 요청을 보냅니다.

예외 처리: 특정 종목 페이지를 긁어오다 네트워크 에러나 타임아웃이 발생하더라도, 전체 프로그램이 멈추지 않고(try-except) 에러 로그만 기록한 뒤 다음 종목으로 넘어갑니다.

[기능 4] StockData 시트에 데이터 누적 기록
상세: 크롤링에 성공한 데이터들을 모아 [ 오늘 날짜 | 종목코드 | 종목명 | 종가 ] 형태로 StockData 시트의 맨 아래 행(Append)에 한 번에 입력합니다. (한 건씩 입력하면 API 호출 제한에 걸릴 수 있으므로 배치로 일괄 입력하여 에러를 줄입니다.)

[기능 5] Gmail 알림 발송
상세: 모든 작업이 끝나면 파이썬의 smtplib 모듈을 활용하여 사용자 구글 메일로 결과를 발송합니다.

내용: "성공 종목: O건 / 실패 종목: X건" 및 실패한 종목 코드를 명시하여 사용자가 즉시 인지할 수 있도록 합니다.

짚고 넘어가야 할 분석 및 피드백 (시간 오류 바로잡기)
GitHub Actions에서 많은 개발자가 겪는 가장 큰 오류 중 하나는 바로 '서버 시간(UTC)' 문제입니다.

깃허브 액션스 서버는 기본적으로 전 세계 표준시인 UTC 기준 시간으로 작동합니다.

한국 시간(KST) 오후 4시에 스케줄러를 돌리려면, 깃허브 설정(Cron)에는 오전 7시로 적어야 합니다.

또한 파이썬 코드 내에서 날짜(오늘 날짜)를 찍을 때도, 단순히 datetime.now()를 쓰면 영국 시간 기준 어제 날짜나 새벽 시간이 찍히는 오류가 발생합니다. 반드시 Asia/Seoul 타임존을 코드에 명시해야 시트에 정확한 한국 날짜가 기록됩니다. 이 부분을 명세에 강력히 고정해 두겠습니다.

'6. 상세 기능 명세'까지 구체화해 보았습니다. 이 내용을 바탕으로 기능 명세를 확정해도 좋을지 검토해 주세요.

다음 단계로 넘어가기 전, 예상 질문 3가지입니다.

타임존 문제(한국 시간 고정)와 배치를 통한 일괄 시트 기록 방식을 기능 명세에 최종 확정할까요?

에러 발생 시 이메일 본문에 '에러 원인(예: 웹사이트 반응 없음)'까지 자세하게 적어주는 기능을 명세에 추가할까요?

상세 기능 명세가 만족스러우시다면, 다음 단계(7. 디자인 가이드)로 넘어갈까요?

확정짓고 넘어가줘

상세 기능 명세를 확정하고, 다음 단계인 '7. 디자인 가이드'로 넘어가겠습니다.

우리가 만드는 프로그램은 GitHub Actions라는 서버 백그라운드에서 실행되기 때문에 별도의 그래픽 GUI(화면) 디자인은 존재하지 않습니다. 하지만, 사용자가 매일 보게 될 구글 시트의 화면 구조 디자인과 이메일 알림의 가독성을 높이기 위한 디자인 가이드를 정의해야 합니다.

7. 디자인 가이드 (구글 시트 및 이메일)
1) 구글 시트 디자인 가이드 (에러 방지 및 가독성)
표 헤더(Header) 시각화: * Dashboard와 StockData 시트의 맨 위 1행(헤더)은 짙은 회색 배경에 흰색 볼드(Bold) 텍스트를 적용하여 데이터 영역과 확실히 구분합니다.

1행은 '행 고정(Freeze 1 row)' 처리하여 데이터가 아래로 길게 쌓여도 헤더가 항상 보이도록 합니다.

종목코드 열(Column) 서식 고정:

Dashboard 시트의 A열(종목코드) 전체는 구글 시트 메뉴에서 [서식] -> [숫자] -> [일반 텍스트]로 강제 지정합니다. (앞자리 0이 사라지는 현상을 시트 자체에서도 이중 방어)

정렬 정규화:

날짜, 종목코드, 종가는 가운데 정렬, 종목명은 좌측 정렬로 통일하여 시각적 안정감을 줍니다.

숫자 표기법:

종가 데이터는 천 단위 구분 기호(쉼표)가 포함된 원화 서식(예: 75,000)으로 자동 표시되도록 StockData 시트의 D열 서식을 지정합니다.

2) 이메일 알림 본문 디자인 가이드 (HTML 메일)
텍스트 기반의 깔끔한 리포트 형태:

밋밋한 줄글 대신 HTML 태그를 사용하여 성공/실패 여부를 직관적으로 보여줍니다.

성공 메세지: 상단에 초록색 글씨로 [성공] MyStockBot 주가 수집 완료 타이틀을 표시합니다.

실패 메세지: 상단에 빨간색 글씨로 [오류 발생] MyStockBot 일부 종목 수집 실패 타이틀을 표시하고, 본문에 빨간색 박스(Table)를 활용하여 에러가 난 종목 코드와 에러 원인을 깔끔하게 표 형태로 나열합니다.

짚고 넘어가야 할 분석 및 피드백 (디자인 시 주의점)
개발자들이 간과하기 쉬운 실수 중 하나가 바로 "구글 시트에 수동으로 서식을 지정해 놨는데, 파이썬이 데이터를 밀어 넣으면서 서식을 깨뜨리는 현상"입니다.

파이썬 gspread 라이브러리로 값을 입력할 때, 단순 값 입력이 아닌 시트 전체 서식을 덮어쓰는 방식으로 코드를 짜면 기껏 지정해 둔 폰트나 정렬, 텍스트 서식이 기본값으로 초기화되어 버립니다.

따라서 파이썬 코드에서는 오직 '값(Value)만 추가'하는 메소드를 사용하고, 시트의 행/열 자체에 서식을 미리 입혀두는 방식으로 디자인 안정성을 확보해야 합니다. 이를 제약 사항 단계에 미리 반영해 두겠습니다.

'7. 디자인 가이드'의 방향성이 마음에 드신다면, 마지막 단계인 '8. 제약 사항'으로 넘어가 프로젝트를 완성 짓겠습니다.

다음 단계로 넘어가기 전, 예상 질문 3가지입니다.

디자인 가이드에 명시된 구글 시트 서식 고정 방식과 HTML 이메일 양식에 동의하시나요?

이메일 알림 발송 시, 매일 성공했을 때도 메일을 받고 싶으신가요? 아니면 에러가 났을 때만 알림 메일을 받고 싶으신가요?

이 내용이 확인되었다면, 마지막 단계(8. 제약 사항)로 넘어가 볼까요?

넘어가줘

디자인 가이드를 확정하고, 드디어 PRD의 마지막 단계인 '8. 제약 사항'으로 넘어가겠습니다.

제약 사항은 프로그램이 정상적으로 작동하기 위해 반드시 지켜져야 하는 환경적 한계나 규칙을 의미합니다. 이 프로젝트가 GitHub Actions와 구글 API를 사용하기 때문에 발생하는 현실적인 제약들을 명확히 짚고 넘어가야 나중에 예기치 못한 에러로 당황하지 않습니다.

8. 제약 사항 (Constraints)
GitHub Actions 환경의 특성을 반영한 객관적인 제약 사항 목록입니다.

1) 실행 시간의 비정밀성 (Cron 지연)
GitHub Actions의 무료 서버(Runner) 스케줄러는 정시 실행을 보장하지 않습니다. 예를 들어, 한국 시간 오후 4시 정각(0 7 * * 1-5)으로 설정하더라도 실제 실행은 구글/깃허브의 서버 대기열 상황에 따라 오후 4시 5분 ~ 4시 30분 사이에 지연되어 실행될 수 있습니다.

따라서 '초 단위, 분 단위의 칼 같은 정시 실행'이 필수적인 서비스에는 부적합하며, '장 마감 이후 저녁 시간대 중 수집 완료'라는 관점으로 접근해야 합니다.

2) 구글 API 호출 제한 (Quota Limits)
구글 시트 API v4는 계정당 분당 요청 횟수 제한(예: 분당 약 300회)이 있습니다.

프로그램이 다중 종목을 수집할 때, 한 종목씩 시트에 데이터를 쓰고 지우는 방식을 쓰면 이 제한에 걸려 프로그램이 뻗어버립니다. 이를 방지하기 위해 파이썬 코드 내에서 데이터를 배열(배치)로 묶어 단 한 번만 시트에 업데이트(Batch Update) 하도록 구현 언어 레벨에서 제약을 두어야 합니다.

3) 네이버 금융 웹사이트 구조 변경 리스크
우리는 네이버 금융의 공식 API가 아닌 '웹 페이지 HTML을 긁어오는 방식(크롤링)'을 사용합니다. 만약 네이버 금융이 사이트를 대대적으로 개편하여 주가가 표시되는 태그의 이름이나 구조를 바꾸면, 프로그램은 주가를 찾지 못하고 에러를 발생시킵니다.

이 경우 프로그램의 결함이 아니므로, 개발자(본인)가 파이썬 코드의 크롤링 타깃 태그 경로를 직접 수정해 주어야 합니다.

4) 보안 비밀키 관리 (GitHub Secrets)
구글 시트에 접근하기 위한 크레덴셜(credentials.json) 파일이나 이메일 비밀번호를 GitHub 코드 저장소(Repository)에 그대로 올리면 전 세계에 내 구글 계정 권한이 유출됩니다.

모든 인증 정보는 반드시 GitHub 저장소의 Settings -> Secrets and variables 메뉴를 통해 암호화된 환경 변수로만 관리해야 합니다.

짚고 넘어가야 할 분석 및 피드백
제약 사항에서 많은 분들이 간과하는 것 중 하나가 바로 '주말 및 공휴일 데이터'입니다.

GitHub Actions는 장이 열리지 않는 토요일, 일요일에도 오후 4시가 되면 정직하게 작동합니다. 이때 네이버 금융에 접속하면 금요일 종가나 직전 거래일의 주가가 그대로 표시되어 있습니다.

만약 주말에도 스크립트가 돌아서 금요일과 똑같은 주가를 토요일/일요일 날짜로 또 저장하게 되면 데이터가 중복되어 자산 그래프가 왜곡됩니다.

따라서 파이썬 코드 시작 부분에 "오늘이 주말(토/일)이면 아무것도 하지 않고 즉시 종료한다"라는 날짜 필터링 제약 조건이 반드시 코드로 구현되어야 에러를 줄일 수 있습니다. 한국의 유동적인 공휴일(설날, 추석 등)까지 완벽히 막으려면 공휴일 달력 API를 연동해야 하지만, 최소한 주말만큼은 무조건 코드 단에서 튕겨내도록 제약해야 합니다.

축하합니다! 이로써 MyStockBot의 전체 PRD(제품 요구사항 정의서) 작성이 모두 완료되었습니다.

우리가 함께 빌드업한 PRD 최종본을 한눈에 보기 쉽게 템플릿 형태로 묶어서 정리해 드릴 준비가 되었습니다. 최종 정리본을 확인하시겠습니까?

주말에는 스케줄 기능이 필요없을 것 같아. 최종 prd 작성해줘

의견을 적극 반영하여 주말 실행 제외 로직을 GitHub Actions 설정과 파이썬 코드 단에 명확히 제약 조건으로 추가했습니다.

그동안 대화를 통해 조율하고 객관적으로 검증한 내용을 바탕으로 완성된 MyStockBot 최종 PRD(제품 요구사항 정의서)입니다.

[PRD] MyStockBot: 주가 데이터 수집 자동화 프로그램
1. 프로젝트 개요
프로젝트 이름: MyStockBot

한 줄 설명: GitHub Actions와 파이썬을 활용하여 매일 장 마감 후 관심 종목의 주가 데이터를 자동으로 수집하고 구글 시트에 누적 기록하는 자동화 프로그램

유형: GitHub Actions 기반 인프라 + 파이썬 배치 스크립트

난이도: 중 (구글 Sheets API 연동 및 웹 크롤링 예외 처리 필요)

2. 사용자 시나리오
대상 사용자: 매일 직접 주가를 확인하고 엑셀에 타이핑하는 번거로움을 줄이고, 자동으로 투자 자산의 주가 트렌드를 축적하고 싶은 국내 주식 투자자.

시나리오:

사용자는 구글 시트의 Dashboard 탭에 원하는 국내 주식 종목코드(6자리)를 입력합니다.

평일(월~금) 오후 4시경, GitHub Actions 스케줄러가 자동으로 깨어나 파이썬 스크립트를 실행합니다.

프로그램은 종목코드를 문자열 타입으로 엄격히 체크하여 네이버 금융에서 당일 종가를 크롤링합니다.

크롤링된 데이터는 구글 시트의 StockData 탭 맨 아래 행에 순차적으로 누적 저장됩니다.

작업이 끝나면 사용자는 스마트폰 이메일(Gmail) 푸시 알림으로 당일 자동 수집 결과를 리포트 형태로 확인합니다.

3. 핵심 기능 목록
다중 종목 설정 및 관리: 구글 시트에 여러 개의 종목 코드를 입력하면 배열 형태로 한 번에 인식하는 기능

주가 데이터 수집: 설정된 다중 종목의 당일 최종 종가를 네이버 금융 모바일 페이지에서 크롤링하는 기능

시트 데이터 누적 기록 (Append): 매일 새로운 행을 추가하여 [날짜 | 종목코드 | 종목명 | 당일종가] 데이터를 순차적으로 누적 저장하는 기능 (API 제한을 고려해 Batch Update 방식으로 구현)

시간 기반 트리거 및 주말 제외: 평일(월~금) 지정된 시간에만 자동 실행되는 스케줄러 기능

오류 예외 처리:

주말 및 공휴일 실행 시 데이터를 수집하지 않고 즉시 종료하는 예외 처리

잘못된 종목 코드 입력 또는 특정 종목 크롤링 실패 시, 전체 프로그램이 멈추지 않고 다음 종목으로 넘어가는 try-except 방어 로직

Gmail 알림 기능: 데이터 수집 완료 후 성공/실패 여부와 에러 원인을 포함한 HTML 이메일 리포트 발송 기능

4. 기술 스택 및 실행 환경
실행 환경: GitHub Actions (무료 클라우드 러너 기반)

개발 언어: Python 3.x

핵심 라이브러리: requests, BeautifulSoup4 (웹 크롤링), gspread (구글 시트 API v4 연동)

데이터베이스: Google Sheets

보안 관리: GitHub Secrets (구글 API 인증 키 및 이메일 계정 정보 암호화 저장)

5. 화면 구성 (구글 시트 구조)
시트 1: Dashboard (사용자 입력 화면)

A열 (종목코드): 수집할 국내 주식 종목코드 입력 (서식을 반드시 '일반 텍스트'로 지정하여 앞자리 0 깨짐 방지)

B열 (종목명): 사용자가 식별하기 위한 종목 이름

시트 2: StockData (데이터 누적 저장소)

구조 (1행 헤더 고정): [ 날짜 | 종목코드 | 종목명 | 당일종가 ]

GitHub Actions가 작동할 때마다 맨 아래 빈 행(Last Row)에 데이터를 순차적으로 추가

6. 상세 기능 명세 (Workflow)
스케줄러 실행: 한국 시간(KST) 기준 월~금 오후 4시(UTC 오전 7시)에 GitHub Actions 가상 환경이 실행됨.

타입 검증: Dashboard 시트의 종목코드를 읽어올 때, 파이썬의 .zfill(6)을 사용하여 무조건 6자리 문자열(String) 형태로 변환 및 검증 처리.

크롤링: 네이버 금융 모바일 주소(https://m.stock.naver.com/item/{종목코드}/total)로 요청을 보내 종가 데이터를 파싱.

일괄 기록: 크롤링 완료된 데이터를 모아 gspread를 통해 단 한 번의 API 호출로 StockData 시트 하단에 일괄 삽입.

시간대 고정: 파이썬 코드 내에 Asia/Seoul 타임존을 명시하여 시트에 기록되는 날짜가 한국 표준시와 일치하도록 보장.

알림 발송: 파이썬 smtplib 모듈을 이용해 지정된 이메일로 결과 전송.

7. 디자인 가이드
구글 시트 서식 보호: 파이썬 코드는 시트에 오직 '값(Value)만 추가'하도록 작성하여, 미리 설정해 둔 시트의 폰트, 정렬, 천 단위 구분 기호(,) 서식이 깨지지 않도록 함.

가독성 정렬: 날짜, 종목코드, 종가는 가운데 정렬, 종목명은 좌측 정렬로 지정.

HTML 이메일 리포트: * 성공 시: 초록색 텍스트로 [성공] 표시 및 수집된 종목 개수 요약.

실패 시: 빨간색 텍스트로 [오류 발생] 표시 및 본문에 에러가 발생한 종목 코드와 에러 원인을 표(Table) 형태로 시각화.

8. 제약 사항
실행 지연 가능성: GitHub Actions 무료 서버의 특성상 설정한 시간보다 최소 10분~최대 1시간까지 실행이 지연될 수 있음.

구글 API 쿼터 제약: 분당 요청 횟수 제한을 넘지 않도록 데이터를 모아서 한 번에 보내는 '배치 업데이트' 방식이 강제됨.

유지보수 리스크: 네이버 금융의 HTML 웹 구조가 변경될 경우 크롤링 에러가 발생할 수 있으며, 이 경우 파이썬 소스코드의 태그 경로 수정이 필요함.

주말 데이터 중복 방지: 요청하신 대로 주말(토/일)에는 작동하더라도 데이터를 수집하지 않고 스크립트를 즉시 안전 종료(sys.exit())하는 로직을 소스코드 상단에 배치해야 함.

이것으로 MyStockBot 개발을 위한 완벽한 설계도(PRD)가 완성되었습니다! 이 PRD를 기반으로 코딩을 시작하시면 삽질 없이 가장 효율적인 자동화 프로그램을 만드실 수 있을 것입니다. 고생하셨습니다.

md 파일로 생성해줘

Your MD file ready
MD 아이콘
 MyStockBot_PRD 
MD

요청하신 대로 완성된 PRD 내용을 마크다운(MD) 파일로 생성했습니다. 위의 파일 링크를 통해 다운로드하여 확인하실 수 있습니다.

해당 PRD를 기반으로 성공적으로 자동화 프로그램을 개발하시기를 바라며, 코드 작성이나 세부 구현 단계에서 도움이 필요하시면 언제든 말씀해 주세요!

Gemini는 AI이며 인물 등에 관한 정보 제공 시 실수를 할 수 있습니다. 개인 정보 보호 및 Gemini새 창에서 열기




# [PRD] MyStockBot: 주가 데이터 수집 자동화 프로그램

## 1. 프로젝트 개요
* **프로젝트 이름:** MyStockBot
* **한 줄 설명:** GitHub Actions와 파이썬을 활용하여 매일 장 마감 후 관심 종목의 주가 데이터를 자동으로 수집하고 구글 시트에 누적 기록하는 자동화 프로그램
* **유형:** GitHub Actions 기반 인프라 + 파이썬 배치 스크립트
* **난이도:** 중 (구글 Sheets API 연동 및 웹 크롤링 예외 처리 필요)

## 2. 사용자 시나리오
* **대상 사용자:** 매일 직접 주가를 확인하고 엑셀에 타이핑하는 번거로움을 줄이고, 자동으로 투자 자산의 주가 트렌드를 축적하고 싶은 국내 주식 투자자.
* **시나리오:**
    1. 사용자는 구글 시트의 `Dashboard` 탭에 원하는 국내 주식 종목코드(6자리)를 입력합니다.
    2. 평일(월~금) 오후 4시경, GitHub Actions 스케줄러가 자동으로 깨어나 파이썬 스크립트를 실행합니다.
    3. 프로그램은 종목코드를 문자열 타입으로 엄격히 체크하여 네이버 금융에서 당일 종가를 크롤링합니다.
    4. 크롤링된 데이터는 구글 시트의 `StockData` 탭 맨 아래 행에 순차적으로 누적 저장됩니다.
    5. 작업이 끝나면 사용자는 스마트폰 이메일(Gmail) 푸시 알림으로 당일 자동 수집 결과를 리포트 형태로 확인합니다.

## 3. 핵심 기능 목록
* **다중 종목 설정 및 관리:** 구글 시트에 여러 개의 종목 코드를 입력하면 배열 형태로 한 번에 인식하는 기능
* **주가 데이터 수집:** 설정된 다중 종목의 당일 최종 종가를 네이버 금융 모바일 페이지에서 크롤링하는 기능
* **시트 데이터 누적 기록 (Append):** 매일 새로운 행을 추가하여 `[날짜 | 종목코드 | 종목명 | 당일종가]` 데이터를 순차적으로 누적 저장하는 기능 (API 제한을 고려해 Batch Update 방식으로 구현)
* **시간 기반 트리거 및 주말 제외:** 평일(월~금) 지정된 시간에만 자동 실행되는 스케줄러 기능
* **오류 예외 처리:**
    * 주말 및 공휴일 실행 시 데이터를 수집하지 않고 즉시 종료하는 예외 처리
    * 잘못된 종목 코드 입력 또는 특정 종목 크롤링 실패 시, 전체 프로그램이 멈추지 않고 다음 종목으로 넘어가는 `try-except` 방어 로직
* **Gmail 알림 기능:** 데이터 수집 완료 후 성공/실패 여부와 에러 원인을 포함한 HTML 이메일 리포트 발송 기능

## 4. 기술 스택 및 실행 환경
* **실행 환경:** GitHub Actions (무료 클라우드 러너 기반)
* **개발 언어:** Python 3.x
* **핵심 라이브러리:** `requests`, `BeautifulSoup4` (웹 크롤링), `gspread` (구글 시트 API v4 연동)
* **데이터베이스:** Google Sheets
* **보안 관리:** GitHub Secrets (구글 API 인증 키 및 이메일 계정 정보 암호화 저장)

## 5. 화면 구성 (구글 시트 구조)
* **시트 1: `Dashboard` (사용자 입력 화면)**
    * **A열 (종목코드):** 수집할 국내 주식 종목코드 입력 (서식을 반드시 **'일반 텍스트'**로 지정하여 앞자리 `0` 깨짐 방지)
    * **B열 (종목명):** 사용자가 식별하기 위한 종목 이름
* **시트 2: `StockData` (데이터 누적 저장소)**
    * **구조 (1행 헤더 고정):** `[ 날짜 | 종목코드 | 종목명 | 당일종가 ]`
    * GitHub Actions가 작동할 때마다 맨 아래 빈 행(Last Row)에 데이터를 순차적으로 추가

## 6. 상세 기능 명세 (Workflow)
1. **스케줄러 실행:** 한국 시간(KST) 기준 월~금 오후 4시(UTC 오전 7시)에 GitHub Actions 가상 환경이 실행됨.
2. **타입 검증:** `Dashboard` 시트의 종목코드를 읽어올 때, 파이썬의 `.zfill(6)`을 사용하여 무조건 **6자리 문자열(String)** 형태로 변환 및 검증 처리.
3. **크롤링:** 네이버 금융 모바일 주소(`https://m.stock.naver.com/item/{종목코드}/total`)로 요청을 보내 종가 데이터를 파싱.
4. **일괄 기록:** 크롤링 완료된 데이터를 모아 `gspread`를 통해 단 한 번의 API 호출로 `StockData` 시트 하단에 일괄 삽입.
5. **시간대 고정:** 파이썬 코드 내에 `Asia/Seoul` 타임존을 명시하여 시트에 기록되는 날짜가 한국 표준시와 일치하도록 보장.
6. **알림 발송:** 파이썬 `smtplib` 모듈을 이용해 지정된 이메일로 결과 전송.

## 7. 디자인 가이드
* **구글 시트 서식 보호:** 파이썬 코드는 시트에 오직 '값(Value)만 추가'하도록 작성하여, 미리 설정해 둔 시트의 폰트, 정렬, 천 단위 구분 기호(`,`) 서식이 깨지지 않도록 함.
* **가독성 정렬:** 날짜, 종목코드, 종가는 가운데 정렬, 종목명은 좌측 정렬로 지정.
* **HTML 이메일 리포트:**
    * 성공 시: 초록색 텍스트로 `[성공]` 표시 및 수집된 종목 개수 요약.
    * 실패 시: 빨간색 텍스트로 `[오류 발생]` 표시 및 본문에 에러가 발생한 종목 코드와 에러 원인을 표(Table) 형태로 시각화.

## 8. 제약 사항
* **실행 지연 가능성:** GitHub Actions 무료 서버의 특성상 설정한 시간보다 최소 10분~최대 1시간까지 실행이 지연될 수 있음.
* **구글 API 쿼터 제약:** 분당 요청 횟수 제한을 넘지 않도록 데이터를 모아서 한 번에 보내는 '배치 업데이트' 방식이 강제됨.
* **유지보수 리스크:** 네이버 금융의 HTML 웹 구조가 변경될 경우 크롤링 에러가 발생할 수 있으며, 이 경우 파이썬 소스코드의 태그 경로 수정이 필요함.
* **주말 데이터 중복 방지:** 주말(토/일)에는 작동하더라도 데이터를 수집하지 않고 스크립트를 즉시 안전 종료(`sys.exit()`)하는 로직을 소스코드 상단에 배치해야 함.
MyStockBot_PRD.md
MyStockBot_PRD.md 항목을 표시하는 중입니다.

---

# [PRD 확장 v2] MyStockBot 웹앱: 3-페이지 대시보드 구조

> 본 확장 명세는 v1(GitHub Actions + 구글 시트 자동 수집 봇)을 기반으로, 이후 실시간 웹 대시보드(React + FastAPI, KIS Open API·yfinance)로 발전한 현재 구현을 반영합니다.
> v1의 자동 수집·시트 기록·이메일 알림 기능은 그대로 유지하며, **웹 프론트엔드의 화면(페이지) 구조를 아래 3개로 재정의**합니다.

## 9. 페이지 구조 개편 (Information Architecture)

기존에는 단일 페이지(`/` = 관심종목 목록)와 상세 페이지(`/stocks/:code`)만 존재했습니다. 이를 **역할이 명확히 분리된 3개 페이지**로 재편합니다.

| 페이지 | 경로(예시) | 역할 | 비고 |
|--------|-----------|------|------|
| **메인 (시장 대시보드)** | `/` | 코스피·코스닥 지수 등 시장 전체 현황을 한눈에 보는 요약 화면 | 신규 |
| **Sub1 (관심종목)** | `/watchlist` | 개별 종목을 등록·검색·조회하고 상세 판정을 확인하는 화면 | 기존 대시보드 기능 이전 |
| **Sub2 (모의 투자)** | `/paper` | 가상 자본으로 매수·매도를 시뮬레이션하는 화면 | 신규 |

* 상세 화면(`/stocks/:code`)은 Sub1의 하위 화면으로 유지합니다(종목 카드 클릭 → 상세 판정·캔들차트).
* 하단 또는 상단에 3개 페이지를 오가는 **탭 네비게이션(GNB)**을 배치합니다. 모바일(PWA) 우선이므로 하단 탭바 형태를 권장합니다.

> **짚고 넘어가야 할 분석 및 피드백 (구조 개편 시 주의점)**
> 현재 `/`(루트) 경로가 곧 관심종목 목록입니다. 메인 페이지를 신설하면서 루트 경로의 의미가 "관심종목"에서 "시장 대시보드"로 바뀝니다. 기존 링크·PWA 시작 URL·즐겨찾기가 관심종목을 가리키고 있었다면, 라우팅 변경 시 **리다이렉트 또는 온보딩 안내**로 사용자 혼란을 줄여야 합니다.

## 10. 메인 페이지 명세 — 시장 대시보드 (확정)

**목적:** 앱을 열자마자 "오늘 시장이 어떤 상황인가"를 3초 안에 파악하게 한다.
**대상 기기:** 모바일 PWA 우선(세로 스크롤), 하단 3-탭 네비게이션.
**색상 규칙:** 상승 = 빨강, 하락 = 파랑(국내 관례). 브랜드 액센트 = 앰버 골드(상승/하락 의미색과 분리). 실시간 표시 = 그린. 라이트/다크 테마 모두 지원.

### 10.1 화면 레이아웃 (위→아래 확정 순서)
> 시각화 목업으로 배치를 확정함(2026-07 리뷰). 각 블록의 우선순위(Tier)는 '13. 추천 확장 기능'과 연동.

1. **앱 헤더** — 로고 + "MyStockBot" + **실시간 배지**(WebSocket 연결 시 그린 펄스)
2. **시장 상태 줄** — `장전/장중/장마감/휴장` 배지 + "마감까지 N시간 M분" + 마지막 갱신 상대시간
3. **지수 카드 2개(핵심·Tier1)** — 코스피·코스닥을 좌우로. 각 카드: 현재지수 / 전일 대비 등락폭(±)·등락률(%) / **당일 흐름 스파크라인**(면적 채움 + 끝점 강조)
4. **시장 폭(Breadth·Tier2)** — 상승·보합·하락 종목 수 비율 바 + 상한가/하한가 개수 (코스피+코스닥 합산)
5. **투자자 매매동향(Tier2)** — 외국인/기관/개인 순매수(+)·순매도(−) 다이버징 바, 단위 억원
6. **내 관심종목 시그널(Tier1)** — 판정 분포 스트립(강력매수/매수/관망/매도/강력매도) + 종목 수. 기존 `DistributionStrip` 재사용
7. **관심종목 Top Movers(Tier1)** — 당일 급등·급락 상위 3종목 미니 리스트(종목명·코드·판정 배지·등락률) → 탭 시 Sub1 상세로 이동
8. **매크로 참고(선택)** — 원/달러 환율, 나스닥(전일) 등. "참고용·전일 기준" 라벨 명시
9. **디스클레이머 푸터** — "기계적 참고 지표 · 투자 권유 아님"

### 10.2 구성 요소별 상세
* **지수 카드:** 등락 부호(▲/▼)와 색을 동시에 사용해 색맹 접근성 보완. 스파크라인은 당일 인트라데이 시계열.
* **시장 상태 배지:** KST 기준 계산. 휴장(주말·공휴일)일 때는 "최근 거래일 기준" 안내로 데이터 신선도 오해 방지.
* **시그널 분포·Top Movers:** 기존 종목 스냅샷(`snapshot`) 데이터를 그대로 활용하므로 추가 데이터 소스 불필요.
* **매크로:** yfinance로 조회. 실시간이 아니므로 반드시 "참고용" 라벨.

> **짚고 넘어가야 할 분석 및 피드백 (지수 데이터 소스)**
> 코스피/코스닥 **지수**·**시장 폭**·**투자자 매매동향**은 개별 종목 시세와 KIS TR ID가 다릅니다. 각각 별도 조회 함수와 캐시가 필요하며, 기존 종목 스냅샷 캐시(`snapshot_cache`)와 동일한 패턴으로 **지수/시장 전용 캐시**를 두고 주기 갱신하는 구조를 권장합니다.
> * **KIS Open API:** 국내업종 현재지수, 등락종목수, 투자자별 매매동향 API 활용.
> * **yfinance 폴백:** `^KS11`(코스피)·`^KQ11`(코스닥), 환율·해외지수. 지연·결측이 있어 폴백/참고용.

## 11. Sub1 페이지 명세 — 관심종목 (기존 기능 이전)

**목적:** 개별 종목을 넣고 보는 화면. 현재 대시보드 기능을 그대로 이전한다.

* **종목 검색/추가:** 종목명·코드로 검색(`/api/stocks/search`) 후 관심종목에 추가.
* **관심종목 카드 목록:** 종목별 현재가, 등락률, 단기/장기 판정, 실시간 틱(WebSocket) 표시.
* **정렬·필터:** 판정순 / 등락률순 / 이름순 정렬, 검색어 필터.
* **판정 분포 스트립:** 등록 종목의 매수/매도 판정 분포 요약.
* **상세 화면 연동:** 카드 클릭 시 `/stocks/:code` 상세(팩터 분해, 볼린저·RSI·MACD, 멀티 타임프레임 캔들차트)로 이동.
* **삭제:** 관심종목 제거(`DELETE /api/watchlist/{code}`).

> 이 페이지는 **신규 개발이 아니라 기존 `DashboardPage`의 이전**입니다. 라우팅 경로만 `/` → `/watchlist`로 바꾸고 컴포넌트를 재사용하면 됩니다.

## 12. Sub2 페이지 명세 — 모의 투자 (Paper Trading)

**목적:** 실제 돈을 쓰지 않고, 실시간 시세를 기준으로 매수·매도를 연습하며 판정 지표의 유효성을 체감한다.

### 12.1 필수 기능
* **가상 계좌(시드머니):** 최초 진입 시 기본 가상 자본 지급(예: 10,000,000원). 초기 금액은 설정 가능.
* **매수 주문:** 관심종목/검색 종목을 선택 → 수량(또는 금액) 입력 → **현재 시세 기준 즉시 체결**(시장가 시뮬레이션). 잔액 부족 시 거부.
* **매도 주문:** 보유 종목의 수량을 선택해 현재가로 매도 체결. 보유 수량 초과 매도 거부.
* **보유 포트폴리오:** 종목별 보유수량, 평균 매입단가, 현재가, 평가금액, **평가손익(±)·수익률(%)** 표시. 현금 잔액과 총 평가자산 합계 노출.
* **거래 내역:** 일시·종목·매수/매도·수량·체결가·금액을 시간순으로 기록.
* **초기화:** 가상 계좌를 초기 상태로 리셋.

### 12.2 추천 기능
* **자산 추이 그래프:** 총 평가자산의 일별 변화를 라인 차트로 시각화.
* **수익률 랭킹/요약:** 실현손익 vs 평가손익 구분, 최고 수익·최대 손실 종목 하이라이트.
* **판정 대비 성과:** "강력매수 판정 종목을 샀을 때의 가상 수익률" 같은 회고 지표로, v1의 판정 로직 유효성을 검증.

> **짚고 넘어가야 할 분석 및 피드백 (체결·데이터 정합성)**
> * **체결 기준가:** "시장가 즉시 체결"은 구현이 단순하지만, 장 마감·휴장 시 마지막 종가로 체결됩니다. 장중/장외 여부에 따라 체결 가능/불가를 명확히 하고, 체결가의 출처(실시간 틱 vs 종가)를 거래 내역에 함께 기록하는 것을 권장합니다.
> * **호가·수수료:** 실거래의 호가 단위·거래 수수료·세금은 초기 버전에서 생략(단순화)하되, "모의 투자는 수수료·슬리피지를 반영하지 않는다"는 안내 문구를 반드시 노출해 실제 수익률과의 괴리를 오해하지 않도록 합니다.
> * **저장 위치:** 가상 계좌·보유·거래 내역은 서버측 영속 저장(기존 watchlist와 동일한 저장소 패턴)을 권장. 브라우저 로컬 저장만 쓰면 기기 변경 시 데이터가 사라집니다.

## 13. 추천 확장 기능 (우선순위별)

사용자 요청("추천해줄 만한 것")에 대한 제안입니다. **필수 아님** — 취사선택 가능.

1. **(강력 추천) 하단 탭 네비게이션:** 3개 페이지 전환용 모바일 탭바. PWA 사용성을 크게 높임.
2. **(강력 추천) 지수 전용 캐시·스케줄러:** 종목 스냅샷과 별도로 코스피/코스닥 지수를 주기 갱신하는 백그라운드 루프(기존 `collector`/`scheduler` 패턴 재사용).
3. **(추천) 모의 투자 자산 추이 그래프:** 학습 동기부여 및 판정 지표 검증에 유용.
4. **(추천) 시장 상태 배지:** 장전/장중/장마감/휴장 표시로 데이터 신선도 오해 방지.
5. **(선택) 참고 해외 지표:** 환율·나스닥·S&P500 전일 종가(yfinance).
6. **(선택) 알림 연동:** v1의 Gmail 알림을 확장해, 관심종목이 특정 판정(예: 강력매수/강력매도)에 진입하면 푸시/이메일 알림.

## 14. 신규 API·데이터 요구사항 (구현 참고)

3-페이지 구조를 뒷받침하기 위해 추가로 필요한 백엔드 요소입니다.

* **지수 조회 API:** `GET /api/indices` → `[{code:"KOSPI", value, change, change_pct}, {code:"KOSDAQ", ...}]`. KIS 지수 API를 1차, yfinance를 폴백으로.
* **모의 투자 API(신규 라우터 `paper`):**
    * `GET /api/paper/account` — 현금 잔액·총 평가자산·보유목록
    * `POST /api/paper/orders` — 매수/매도 주문(`{code, side, qty}`), 현재가 기준 체결
    * `GET /api/paper/trades` — 거래 내역
    * `POST /api/paper/reset` — 계좌 초기화
* **영속 저장:** 가상 계좌/보유/거래 내역 저장소(기존 watchlist 영속화 패턴 준용).
* **인증:** 기존 Bearer 토큰 미들웨어 범위에 신규 엔드포인트 포함.

> 위 확장은 v1 PRD의 목표(자동 수집·기록·알림)를 대체하지 않으며, 그 위에 얹는 **웹 사용자 경험 계층**입니다. v1의 GitHub Actions 배치 수집은 계속 데이터의 기반이 됩니다.

---

# [PRD 확장 v3] 차별화 전략 · 기술 보완 · 사용자 편의성

> 본 확장은 실제 코드베이스 감사(2026-07, `server/`·`src/`·`web/` 정독)를 근거로, "왜 이 앱을 써야 하는가"를 정의하고 기술·UX 보완 로드맵을 명세화합니다. 모든 항목은 실제 파일 근거 기반입니다.

## 15. 차별화 전략 — "써야만 하는 이유"

### 15.1 현황 진단
* **상대적으로 강한 것:** 자체 **기계적 종합 판정 로직**(`src/indicators.py`의 단기 60분봉·장기 일봉 5단계 판정)과 **판정 근거의 팩터별 점수 분해**(`web/src/utils/factorScoring.ts` + `FactorBreakdown.tsx`). 국내 리테일 앱에 드문 자산.
* **약한 것(따라잡기):** 실시간 틱·멀티 타임프레임 캔들·지수 대시보드·모의투자 그 자체 — 트레이딩뷰/MTS/토스증권이 구조적으로 더 잘하는 영역. 차별화 엔진이 아니라 기본기.
* **결론:** 현재 "써야만 하는 이유"는 약함. **자체 판정 로직 + 이미 누적 중인 캔들 이력(`db.candles`) + 팩터 분해 + 모의투자**를 엮어 "**판정의 신뢰성·설명·검증**" 계층으로 올라서야 함.

### 15.2 ⭐ 시그니처 기능 — 판정 백테스트 ("이 앱 판정대로 샀으면?")
* **정의:** 종목별로 과거 각 시점에 판정 로직을 재적용해 **적중률·가상 누적 수익률·평균 수익**을 카드/차트로 제시. 예: "최근 1년간 장기 판정을 그대로 따랐다면 가상 누적 +X%, 적중률 Y%".
* **실현 가능성(높음):** `db.candles`에 OHLC 이력이 이미 read-through로 누적 중 → 과거 df 슬라이스에 기존 판정 함수(`macd_cross_signal`/`rsi_zone_signal`/`short_term_view`/`long_term_view`)를 재호출. 체결·손익은 모의투자(Sub2) 체결 로직 재사용. **신규 외부 데이터 소스 0개.**
* **범위 주의:** 재무 지표(PER/PBR/ROE)의 과거값은 결측이 많으므로, **초기 버전은 기술적 판정 백테스트로 한정** 출시. "과거 성과는 미래를 보장하지 않으며 수수료·슬리피지 미반영" 디스클레이머 필수. 종목당 재적용 표본이 적으면 "적중률 X%"가 통계적으로 무의미할 수 있으므로 표본 수·기간 병기.

### 15.2b 경쟁 현황 — 정직한 진단 (2026-07 실증 조사)
> **주의:** "이 기능은 어디에도 없다 / 자체 IP라 복제 불가"는 **사실이 아니다.** 4개 경쟁 카테고리(국내 MTS·국내 퀀트·글로벌 등급·글로벌 소비자앱)를 웹 근거로 조사하고 적대적 검증한 결과, 판정=`partially-exists`(글로벌 거의 존재 / 국내 드묾)로 나왔다.

* **글로벌 — "자체 판정 + 그 판정의 과거 성과 공개 + 개인 노출"은 이미 상용화됨:**
    * **Zacks Rank**: 1~5등급 + "Rank #1대로 샀다면 1988년~ 연 +23.5%" 무료 공개
    * **TipRanks Smart Score**(1~10) / **Seeking Alpha Quant**(5단계): 등급별 과거 성과 공개
    * **Danelfin**(AI Score 1~10) / **Tickeron**: 자체 AI 등급 + 백테스트 승률·수익률을 초보에게 노출
    * **TradingView**: 자체 Technical Rating을 **Strategy Tester로 종목별 과거 재적용**까지 지원(재적용 발상 자체가 이미 존재)
* **국내 — 드묾(rare):** 조사한 한국 앱 중 "자체 5단계 종합 판정 + 종목별 백테스트 + 초보 원탭"을 그대로 결합한 곳은 없음. 증권사 MTS는 대부분 셀사이드 컨센서스 노출(키움 신호검색이 개별 기술신호로 근접), 퀀트 서비스(젠포트·인텔리퀀트·퀀트킹)는 백테스트 대상이 "사용자 전략"이지 앱 자체 판정이 아니며 초보 원탭 아님, 로보(콴텍·파운트·핀트)는 위탁운용이라 자가검증 도구가 아님.
* **진짜 차별점(정직한 버전):** "세계 최초/유일"이 **아님.** 우리가 다른 건 **결합·패키징**뿐 — *"관심종목 개별 종목에 기술적+밸류 5단계 판정을 재적용한 종목별 적중률을, 한국 종목 대상으로, 초보에게 원탭으로"*. 이는 기술 해자가 아니라 UX·로컬라이징 차이.
* **방어 해자 재정의:** 기능 자체는 non-defensible(대형사가 3~6개월 내 유사 탑재 가능, 시세 데이터는 토스·키움이 더 우위). 실제 방어선은 → **① 초보 원탭 UX 완성도 ② 한국 종목·회계·세제 로컬라이징 ③ 판정 룰의 라이브 실적 트랙레코드 축적 ④ 브랜드·유저 선점.** 포지셔닝을 "복제 불가"가 아니라 **"먼저, 제대로, 국내 초보 맞춤으로 선점"**으로 잡아야 정직하다.

### 15.2c 🚀 그로스 기능 — 적립식 백테스트 (DCA 시뮬레이터) + 공유 카드
> 판정 백테스트(§15.2)가 **신뢰·리텐션**용이라면, 적립식 백테스트는 **바이럴 획득(top-of-funnel)**용이다. 쇼츠·릴스에서 흔한 *"10년간 매달 1주씩 샀다면 지금 +N%"* 포맷을 앱 기능으로 제품화한다.

* **정의:** 판정 로직 **없이** 정기 매수만 시뮬레이션. 사용자가 종목·주기·방식·기간을 고르면 "누적 수익률 +N%, 투자 원금 X원, 평가금액 Y원, 수익 Z원"을 **공유 카드(쇼츠 세로 포맷)**로 출력.
* **입력:**
    * 종목/지수 (예: 나스닥100·삼성전자·S&P500)
    * 매수 주기 — 매주 / 매월 / 매분기
    * 매수 방식 — **정량(매월 1주씩)** / **정액(매월 N원어치, 소수점 체결 시뮬)**
    * 기간 — 3/5/10년 또는 커스텀
    * 배당 재투자 ON/OFF
* **출력(공유 카드):** 히어로 수익률(+N%), 원금·평가금액·수익, 평가금액 vs 누적 원금 시계열(면적+기준선), 매수 횟수·평균단가·기간, 브랜드·면책 문구, "내 종목으로 해보기" CTA, 원탭 이미지 공유.
* **왜 그로스에 강한가:** 판정을 몰라도 이해되는 초직관 포맷 → SNS 공유·유입 유도. 카드에 앱 브랜드/CTA가 박혀 **자연 유입 루프**가 된다.
* **실현 가능성(중):** 계산은 DCA라 매우 단순(매 주기 매수→누적 주수·원가→최신가 평가). **단, 장기 월봉 데이터가 선결.** `db.candles`는 read-through로 앞으로만 누적되어 10년치가 아직 없음 → **국내는 KIS 월봉(수정주가), 해외·지수는 yfinance**(`QQQ`·`^NDX`·`^GSPC`)로 장기 시계열을 조회해야 함.
* **정직한 가정·한계(카드에 명시 필수):** 수수료·세금·슬리피지 미반영 / 수정주가(분할·병합 반영) / 배당 재투자 vs 가격수익 구분 / **해외는 환율 영향 포함(달러 기준 별도 표기)** / 과거 성과는 미래 보장 아님 + 시작 시점 민감.

### 15.3 보조 차별화 기능
* **판정 근거 자연어 브리핑:** 점수 바를 한 문장으로 자동 요약("장기 매수: ROE 18%(+1)·PBR 0.8배(+1)·MACD 골든크로스(+2) → 총 +4점"). `buildFactorRows` 결과 재사용, 난이도 하.
* **판정 전환 알림 엔진:** "강력매수 진입", "관망→매수 전환", "RSI 30 이하" 등 사용자 규칙 충족 시 알림. `collector`가 이미 전 종목 판정을 주기 산출 → 직전 사이클과 diff. v1 `notifier` + 웹 푸시 재사용.
  > **구현 시 정정(§20 참고):** "직전 사이클과 diff" 는 잘못된 설계다 — 재시작마다 전 종목 알림이 터지고 판정 왕복 시마다 알림이 나간다. 실제 구현은 **"마지막으로 알린 판정"과 비교**(SQLite 영속)한다. 사용자 정의 규칙과 웹푸시는 아직 미구현.
* **판정 전환 타임라인(Signal History):** 종목 상세 캔들차트 위에 매수/매도 전환 시점 마커 오버레이. 시그니처 기능의 시각적 프론트엔드.
* **초보자 코칭 모드:** 지표 용어 툴팁 사전 + 판정 5단계 의미 설명. "지표를 몰라도 쓰는 판정 앱" 포지션.

### 15.4 포지셔닝 태그라인(후보)
1. **"판정하고, 증명한다."** — 근거와 과거 성적표까지 보여주는 국내주식 판정 앱
2. **"이 앱 말대로 샀으면?"** — 판정을 백테스트로 검증하는 주식 코치
3. **"왜 사야 하는지까지 알려주는 종합 판정."**

## 16. 기술 보완 로드맵

### 16.1 P0 (치명/필수)
* **KIS 토큰/approval_key 캐시 동시성 락:** `src/kis_auth.py`의 `_TOKEN_CACHE`/`_APPROVAL_KEY_CACHE`가 락 없는 dict → 부팅·만료 직후 콜드미스 시 여러 스레드(candles 요청·WS·collector·크론)가 동시에 발급 → KIS 발급 rate-limit(EGW00133) 실패. **double-checked locking**(전역 `threading.Lock`)으로 발급 구간 보호 + rt_cd/error_code 검사 백오프. 지수 API 등 신규 기능의 선결 조건.
* **`bar_history` 데이터 흐름 정리:** `db.save_daily_bars()`의 유일 호출자가 백필 스크립트뿐이고 상시 서버·크론이 적재하지 않아 서버 `bar_history`가 고아 상태(`db.get_bar_history()`는 실질 死코드). 죽은 코드 제거 **또는** collector 사이클에서 일봉 적재 연결. 데이터 소유 경계 문서화(시트=크론 리포트, SQLite=웹앱).

### 16.2 P1 (중요)
* **WebSocket read 워치독:** `kis_ws._read_loop`가 `async for`로 무기한 대기 + `ping_interval=None` → TCP half-open/PINGPONG 중단 시 좀비 연결(배지는 초록인데 틱 없음). `asyncio.wait_for(recv, timeout)` 기반으로 무수신 시 강제 재연결(PINGPONG 주기 2배 기준, 장외 오탐 방지).
* **테스트·린트·타입체크·CI 도입:** 저장소에 테스트/린트/타입설정 0개. 파서(`kis_ws` 46필드 청킹)·지표·`_normalize_code` 단위테스트 + `ruff`/`mypy`/`tsc --noEmit`/`vite build` PR CI 잡. 의존성 상한 핀(현재 `>=` 미고정).
* **모의투자 주문 원자적 트랜잭션(신규 선결):** `db.py`가 호출마다 새 연결·문장별 commit → `POST /api/paper/orders`의 read-modify-write가 동시 주문 시 TOCTOU 레이스(초과매수/음수 잔액). `BEGIN IMMEDIATE` 단일 트랜잭션으로 검증·갱신 원자화. 체결가 출처·장중/장외 함께 저장, **서버측 영속(localStorage 금지)**.
* **지수 전용 수집 루프/캐시(신규 선결):** 지수·등락종목수·투자자 매매동향은 개별 종목과 다른 KIS TR ID → `collector.py` 패턴 복제한 `indices_cache` + `GET /api/indices`. 종목보다 긴 갱신 주기(장중 30~60초), 토큰 락(P0) 선행.

### 16.3 P2 (개선)
* **휴장일 인지:** `collector._is_market_hours`가 평일·시간만 판단, 공휴일 미인지 → 불필요 조회 + "휴장" 배지 데이터 근거 부재. 공휴일 캘린더 도입.
* **KIS rate-limit 전역 조율:** `KIS_RATE_LIMIT_DELAY`가 스레드 로컬 `sleep`인데 `ThreadPoolExecutor(max_workers=4)` 병렬 → 초당 버스트가 의도의 수 배. 전역 토큰버킷/세마포어.
* **in-memory 상태 재시작 유실:** collector·재무·소스 캐시가 프로세스 메모리 → 재시작 시 첫 사이클 전까지 빈 스냅샷. 부팅 워밍 또는 프론트에 첫 사이클 지연 명시.
* **보안 인지 항목:** WS 토큰 `?token=` 쿼리 노출(액세스로그), 프론트 `localStorage` 토큰(XSS), 마스터 다운로드 `verify=False`, `/docs` 무인증. 개인용이라 수용 가능하나 문서화 + 프로덕션 문서 라우트 인증/비활성 권장.
* **관측성/헬스체크:** `print()` 산재·타임스탬프 없는 로깅 → 표준 `logging` 통일. 헬스체크가 liveness만 → `/api/health`에 WS 상태·마지막 스냅샷 신선도(readiness) 추가.
* **배포:** 컨테이너 root 실행 → 비특권 유저. 크론 미고정 의존성·무캐시 `pip install` → 핀 + pip 캐시. (`--workers 1`은 in-memory 상태·WS 구조상 필수 = 수평 확장 한계로 명시.)

## 17. 사용자 편의성(UX) 개선 로드맵

### 17.1 P0 (첫 사용·핵심 차단)
* **토큰 배너 온보딩:** `TokenBanner.tsx`가 "토큰 필요" 문구+입력뿐 — 발급 위치·형식·예시·도움말 전무. 최초 사용자가 앱을 한 발짝도 못 나감. 발급 안내 링크·형식 placeholder·show/hide 토글·검증 피드백 추가.
* **PWA manifest + service worker:** 아이콘 4종은 있으나 `manifest.webmanifest`·SW 부재 → 설치 프롬프트·오프라인·standalone 불가(§10 "모바일 PWA 우선"과 배치). manifest(start_url·display:standalone·theme_color) + `index.html` 링크 + 오프라인 셸 SW. 웹 푸시(§13-6)의 전제.

### 17.2 P1 (핵심 가치·신뢰·접근성)
* **판정 점수 근거 설명:** `DecisionGauge`/`FactorBreakdown`이 숫자·막대만 노출, 산정 규칙(골든크로스=+2 등) 미설명 → 핵심 가치 전달 실패. 팩터 툴팁 + "합계가 임계 이상이면 매수" 캡션. (15.3 자연어 브리핑과 연계)
* **삭제 UX:** 네이티브 `window.confirm` + 서버 응답 대기 후 재조회 → 낙관적 제거 + "실행 취소" 토스트, 인앱 확인 UI.
* **로딩 스켈레톤:** 스냅샷 로딩 중 카드가 "—" 투성이. 카드 그리드 스켈레톤.
* **연결 실패 재시도:** "서버에 연결할 수 없습니다" 배너에 버튼 없음 → 이미 있는 `snapshot.refresh()` 연결한 "다시 시도" + 헤더 새로고침/pull-to-refresh.
* **검색/필터 의도 분리:** `query` 하나가 신규 추가 검색과 목록 필터를 겸함 → "내 목록이 사라졌다" 오해. 드롭다운 열림 시 목록 필터 미적용 또는 필터 칩 명시.
* **색 대비(a11y):** 매수 라임칩(#65a30d) 흰 글자 대비 ~3.2:1로 WCAG AA 미달. 5단계 팔레트 대비 재점검. (등락 ▲/▼ 기호 병기는 양호.)
* **3-페이지·하단 탭 셸 구현:** 현재 `App.tsx`에 `/`·`/stocks/:code` 2개뿐 → §9의 메인/Sub1/Sub2·하단 GNB 미존재. 탭 셸 + 라우트 스캐폴딩, `/` 의미 변경 리다이렉트.

### 17.3 P2 (완성도)
* **다크 테마:** `index.css`가 `color-scheme: light` 고정, `theme-color`는 다크 네이비라 불일치 → `prefers-color-scheme` 다크 토큰 세트.
* **heading 계층:** 대시보드 타이틀이 `<span>`이라 h1 부재 → 스크린리더 목차 붕괴. h1/h2 정렬.
* **터치 타깃 44px:** 삭제(28px)·정렬·타임프레임 버튼 확대.
* **카피 통일:** "Watchlist"/"Real-time"/"Indicator Analysis" 등 영문 혼재 → 한국어 우선 통일(지표 약어 병기).
* **백그라운드 폴링 정지:** `useSnapshot` 20초 폴링이 숨은 탭에서도 지속 → `visibilitychange` 연동.
* **상세 페이지 빈 카드:** 미등록 종목에도 분석 카드 4개 렌더 → 숨기고 "관심종목에 추가" CTA.

## 18. 통합 우선순위 (권장 착수 순서)
1. **[기술 P0]** KIS 토큰 락 → 이후 모든 신규 조회 기능의 안전판
2. **[UX P0]** 토큰 온보딩 + PWA manifest/SW → 첫 진입·리텐션 토대
3. **[구조]** 3-페이지 + 하단 탭 셸 스캐폴딩 (§9)
4. **[기능]** 지수 API·캐시(§10·§16.2) → 메인 대시보드 실데이터
5. **[기능]** 모의투자 라우터(원자적 트랜잭션, §12·§16.2)
6. **[차별화]** ⭐ 판정 백테스트(§15.2) — 시그니처, 신규 데이터 소스 0
7. **[UX P1]** 판정 근거 설명·삭제 UX·재시도·스켈레톤

# [구현 현황] v3 로드맵 반영 상태 (PR #15 기준, 2026-07)

> 본 절은 v2/v3 명세 대비 **실제 구현된 범위**를 기록한다. `[x]` 완료 · `[~]` 부분 · `[ ]` 미착수. 근거는 브랜치 `claude/mystockbot-feature-spec-c0l3pb` 커밋.

## 19. 구현된 항목

### 19.1 구조·페이지
* `[x]` **3-페이지 + 하단 탭 셸** (§9·§17.2·§18-3) — `web/src/App.tsx` 라우트(`/`=메인·`/watchlist`=Sub1·`/paper`=Sub2·`/stocks/:code`), `TabBar.tsx`·`TabLayout.tsx`.
* `[~]` **메인 대시보드** (§10) — `MainDashboardPage.tsx` + `useIndices.ts`.
  ※ 이전 판에서 `[x] 완료`로 표기했으나 실제 코드와 불일치 → 정정(2026-07).

  | §10.1 블록 | 상태 |
  |---|---|
  | 1. 앱 헤더 + 실시간 배지 | `[x]` |
  | 2. 시장 상태 줄(장전/장중/장마감/휴장 + 카운트다운 + 갱신 시각) | `[x]` `market_calendar.market_status` + `GET /api/market/status` + `MarketStatusBar` |
  | 3. 지수 카드 2개 | `[x]` 값·등락 / `[ ]` **당일 흐름 스파크라인** |
  | 4. 시장 폭(등락종목수 비율 바 + 상한/하한) | `[x]` KIS 현재지수(FHPUP02100000) + `MarketBreadthBar` |
  | 5. 투자자 매매동향(외국인/기관/개인 다이버징 바) | `[ ]` TR 검증 완료(FHPTJ04030000) · 미구현 |
  | 6. 관심종목 판정 분포 스트립 | `[x]` |
  | 7. 관심종목 Top Movers | `[x]` |
  | 8. 매크로 참고(환율·나스닥 전일) | `[ ]` |
  | 9. 디스클레이머 푸터 | `[x]` |

  블록2 부수 효과: 휴장·장전에는 "N월 N일 거래일 기준"을 명시해 데이터 신선도 오해를 막는다.
  블록4 부수 효과: 현재지수 API 가 전일 대비를 **직접** 주므로, 기존 일자별 지수 경로(종가 2개 차감)보다
  정확하다. 3단 폴백(현재지수 → 일자별 지수 → yfinance)이며 시장 폭은 1순위에서만 나오므로
  폴백 시 `breadth=None` 이고 화면은 그 블록을 아예 숨긴다(0으로 채운 빈 바를 그리면
  "오늘 아무 종목도 안 움직였다"로 오해된다).
* `[x]` **Sub2 모의투자 페이지** (§12) — `PaperTradingPage.tsx` (실주문·보유·거래내역·초기화).
  단, §12.2 추천 기능(자산 추이 그래프·수익률 랭킹·판정 대비 성과)과 실현손익 구분·체결가 출처 기록은 미구현.

### 19.2 차별화 기능
* `[x]` **⭐ 판정 백테스트** (§15.2·§18-6) — `server/services/backtest.py` + `GET /api/stocks/{code}/backtest`, `BacktestCard.tsx`. 기술적 판정(MACD+RSI)만, 재무 None. 지표 1회 계산(리뷰 M2).
  `[x]` **표본 신뢰도 표기(2026-07 추가)** — 적중률 95% 신뢰구간(Wilson) + 겹침 보정 독립 표본 수(`effective_signals`) + `low_confidence` 배지 + `bars_used`/`bars_available`/`truncated` 노출 + 가정·한계 `notes`. 재무 미반영을 응답 필드(`fundamentals_included`)와 notes 양쪽에 명시.
* `[~]` **🚀 적립식 백테스트(DCA)** (§15.2c) — `server/services/dca.py` + `GET .../dca`, `DcaCard.tsx` (정량/정액 모드).
  **미구현: 공유 카드(쇼츠 세로 포맷 이미지·원탭 공유) — §15.2c 의 그로스 핵심 장치.**
  **미지원: 해외·지수 종목(QQQ·^NDX·^GSPC) — `db.normalize_code` 가 6자리 국내코드를 강제.**
  배당 재투자 토글은 UI에 있으나 실제 반영되지 않고 `notes` 로 미반영을 고지한다(§20 참고).

### 19.3 기술 보완
* `[x]` **KIS 토큰/approval_key 동시성 락 (P0)** (§16.1) — `src/kis_auth.py` double-checked locking.
* `[x]` **지수 전용 캐시 + `GET /api/indices` (P1)** (§16.2) — `server/services/indices.py` (KIS FHKUP03500100 1차 → yfinance 폴백, read-through 캐시, 날짜 파싱 방어=리뷰 M1).
* `[x]` **모의투자 원자적 트랜잭션 (P1)** (§16.2) — `db.execute_paper_order` `BEGIN IMMEDIATE`, 서버측 영속(SQLite).

### 19.4 UX
* `[x]` **토큰 배너 온보딩 (P0)** (§17.1) — `TokenBanner.tsx` 발급 안내·형식·show/hide 토글.
* `[x]` **PWA manifest/SW (P0)** (§17.1) — `web/vite.config.ts` `VitePWA`(빌드 시 생성).
* `[x]` **삭제 UX — 낙관적 제거 + 실행취소 토스트** (§17.2) — `DashboardPage.tsx`.
* `[x]` **로딩 스켈레톤** (§17.2) · `[x]` **연결 실패 재시도 버튼** (§17.2).
* `[x]` **검색/필터 의도 분리** (§17.2) — `AddStockForm.tsx` 자체 검색 상태 + 목록 `filter` 분리 + 필터 칩.
* `[x]` **색 대비(a11y) 매수칩** (§17.2) — `#65a30d→#4d7c0f`.
* `[x]` **상세 페이지 빈 카드 정리 + CTA** (§17.3) — `StockDetailPage.tsx`.
* `[x]` **다크 테마** (§17.3) — `index.css` `prefers-color-scheme` 토큰 세트.

## 20. 후속 항목 진행 현황
* `[x]` **`bar_history` 死코드 정리/연결 (P0)** (§16.1) — 고아 테이블 제거 + 백필을 `candles`로 연결.
* `[x]` **판정 근거 자연어 설명·툴팁 (P1)** (§17.2·§15.3) — `FactorBreakdown` 규칙 설명·요약 캡션 + `DecisionGauge` 임계 규칙 명문화.
* `[x]` **테스트·린트·CI (P1)** (§16.2) — pytest + ruff + GitHub Actions CI(backend·web). **2026-07 확장: 51건**(kis_ws 46필드 파서·워치독, 시트 동기화 규칙, `bar_date` 파싱, 백테스트 신뢰도).
* `[x]` **WebSocket read 워치독 (P1)** (§16.2) — `kis_ws._read_loop` 를 `asyncio.wait_for(ws.recv(), timeout)` 기반으로 전환(`_READ_IDLE_TIMEOUT_SECONDS=180`). 무수신 시 `_ReadIdleTimeout` → 연결 종료 → 재연결. "초록 배지 + 틱 0" 좀비 연결 해소.
* `[x]` **로깅 전면 통일 (P2)** (§16.3) — `server/`·`src/`·`scripts/` 의 `print()` 전량(59건) 제거 → 모듈별 `logging.getLogger(__name__)`. 엔트리포인트(`server/main.py`·`src/main.py`·`scripts/*`)가 `basicConfig` 로 타임스탬프·레벨·로거명 포맷을 책임진다. 실패 섞인 수집 사이클만 WARNING.
* `[x]` **크론 휴장일 인지 (P2 보강)** (§16.3·§8) — `src/main.py` 가 `market_calendar.is_trading_day()` 사용(기존엔 `weekday()` 만 검사). 2차 방어선으로 **시트 `날짜` 컬럼을 실행일이 아닌 거래일(`crawler._bar_date`)로 기록** → 달력 미등록 휴장일에도 `(날짜,종목코드)` 중복 스킵이 걸린다. 표 범위 밖 연도는 경고 로그(`market_calendar.TABLE_MAX_YEAR`).
* `[x]` **관심종목 단일 소스화** (신규) — 웹앱 SQLite ↔ 크론 Google Sheets `Dashboard` 이원화 해소. `src/watchlist_sync.py`: 앱→시트 즉시 미러(추가=upsert, 삭제=C열 `해제` 표시로 비파괴), 시트→앱 주기 임포트(부팅 + 평일 매시 :50, **추가 전용**), `POST /api/watchlist/sync` 수동 트리거. 자격증명 없으면 조용히 비활성.
* `[x]` **hit_rate 신뢰구간·`_MAX_BARS` 캡 표기 (감사 MEDIUM)** — §19.2 참고.
* `[x]` **KIS 호출 지연 이중 적용 제거** — `collector._fetch_daily` 의 `time.sleep(KIS_RATE_LIMIT_DELAY)` 삭제(호출 간격은 `kis_auth.kis_throttle()` 이 전역 보장).
* `[x]` **P2 기술** (§16.3) — 휴장일 인지·KIS rate-limit 전역 조율·헬스 readiness·로깅 통일,
  그리고 배포 하드닝(컨테이너 비특권 유저·의존성 핀·이미지에 UI 포함)까지 완료(§20 참고).
* `[~]` **P2 UX** (§17.3) — heading 계층·터치 타깃·카피 통일·백그라운드 폴링 정지 완료.
* `[ ]` **배당 실제 반영 (DCA reinvest)** — 배당 시계열 데이터 소스 연동 필요(현재 미반영 고지만).
* `[~]` **메인 대시보드 잔여 블록** (§10.1) — 시장 상태 줄·시장 폭 완료. **남은 것: 지수 스파크라인, 투자자 매매동향, 매크로 참고.**
  투자자 매매동향은 TR 을 이미 검증했다(아래).
* `[x]` **공식 휴장일 API 도입** — `CTCA0903R`(국내휴장일조회, `chk-holiday`, `BASS_DT`)
  기반으로 `market_calendar.is_trading_day` 를 **캐시 우선** 구조로 바꿨다.
  하드코딩 표(2026까지)는 자격증명 없음·캐시 미보유 구간의 폴백으로 남는다.
  · 개장일 판단은 `opnd_yn` — 공식 docstring 이 지정한 필드.
  · **공식 문서가 "가급적 1일 1회 호출"을 요청**(원장 서비스 연관)하므로 결과를 SQLite
    `market_holidays` 에 영속 저장하고 `KIS_HOLIDAY_MIN_REFRESH_HOURS`(기본 20시간)
    이내면 호출조차 하지 않는다. 부팅 1회 + 매일 08:10 스케줄이 겹쳐도 실제 호출은
    하루 한 번으로 수렴한다.
  · 응답의 `calendar_source`("kis"|"builtin")로 판정 근거를 화면에 노출한다.
  · 실측 확인: 2027-02-08 은 하드코딩 표만으로는 거래일로 오판되지만, 캐시가 있으면
    휴장으로 바로잡힌다.
  · **크론(GitHub Actions)은 이 캐시의 혜택을 받지 못한다** — 러너가 매번 새로 클론되어
    DB가 비어 있다. 크론의 방어선은 여전히 하드코딩 표 + 거래일 기준 기록(`bar_date`)이다.
* `[ ]` **투자자 매매동향** (§10.1 블록5) — TR 검증 완료: `FHPTJ04030000`,
  `/uapi/domestic-stock/v1/quotations/inquire-investor-time-by-market`,
  파라미터 `FID_INPUT_ISCD`(시장구분)·`FID_INPUT_ISCD_2`(업종구분), 공식 예제의 유일한
  실증 조합은 `("999", "S001")`. 응답에 `frgn_ntby_tr_pbmn`(외국인 순매수 거래대금)·
  `prsn_ntby_tr_pbmn`(개인)·`orgn_ntby_tr_pbmn`(기관계)이 있어 다이버징 바에 바로 쓸 수 있다.
* `[ ]` **DCA 공유 카드·해외 종목** (§15.2c) — 그로스 루프의 핵심이 아직 없음.
* `[~]` **판정 전환 알림 엔진** (§15.3·§13-6) — `server/services/alerts.py` +
  `src/alert_channels.py`(Slack) + `notifier.send_html`(Gmail) + `/api/alerts/{config,state,test}`.
  **미구현: 웹푸시, 사용자 정의 규칙("RSI 30 이하" 등).** 전환 이력은 §20 에서 구현했다.

  §15.3 은 이 기능을 "직전 사이클과 diff" 로 적었는데, **그렇게 만들면 안 된다는 것을
  구현 중에 확인했다.** 직전 사이클과 비교하면 재시작마다 전 종목 알림이 터지고 판정이
  A↔B 로 흔들릴 때마다 알림이 나간다. 그래서 비교 기준을 **"마지막으로 알린 판정"**(SQLite
  `decision_alert_state` 영속)으로 바꿨다 — 재시작에 안전하고 왕복 플랩이 저절로 억제된다.

  실제로 확인한 오알림 경로와 대응(전부 `tests/test_alerts.py` 에 테스트 있음):
  · **판정 없음** — `collector._error_item` 은 판정을 `None` 으로 채우고 60분봉 결측은
    `데이터부족`을 낸다. 알리면 수집 실패가 매매 신호로 보인다. 더 미묘한 것: 지표 봉이
    모자랄 때는 `데이터부족`이 아니라 **0점=관망**이 나오므로(MACD 35봉·RSI 15봉),
    35번째 봉이 쌓이는 순간 가짜 '관망→매수' 전환이 생긴다.
  · **재무 지연 도착** — 재무는 6시간 캐시라 부팅 몇 사이클 뒤 채워지고, 채워지면 장기
    판정이 재무 점수(±3)만큼 통째로 이동한다. "첫 사이클을 기준선으로" 만으로는 막히지
    않는다(2~3번째 사이클에 터진다) → `fund_present` 변화 사이클은 무음 재시딩.
  · **출처 전환** — `kis↔yfinance` 가 바뀌면 라벨이 바뀔 수 있다. 시장이 아니라 입력이
    바뀐 것이므로 같은 처리.
  · **골든크로스의 구조적 강등** — 교차는 한 봉만 성립하므로 강력매수(+2)는 **다음 봉에
    필연적으로** 매수(+1)로 내려앉는다. 즉 골든크로스마다 두 번째 알림이 자동으로 따라온다
    (매도측 대칭). → 기본값을 **측(매수/관망/매도) 변화만 알림**으로 두었다.
  · **주말 새벽 알림** — 유휴 사이클(600초)이 60분봉 신선도 기준(300초)보다 길어 주말·야간에도
    매 사이클 외부 재조회가 돈다. 야후 실패→복구가 라벨 재계산을 일으킨다 → 거래일 정규장
    (09:00~15:30)만 알린다.
  · **발송 실패** — 실패한 발송으로 기준선을 옮기면 그 전환은 영구 유실된다 → 한 채널이라도
    성공했을 때만 기준선을 옮기고, 실패 시 히스테리시스 카운터를 유지해 다음 사이클에 재시도.

  성능·안전 제약도 코드로 잠갔다: 발송은 `collector._state_lock` **밖에서** 한다(그 락은
  이벤트 루프 스레드가 직접 잡으므로 안에서 I/O 를 하면 서버 전체가 멈춘다), 그리고 이
  경로에서 **KIS 를 다시 부르지 않는다**(`kis_auth.kis_throttle()` 이 전역 락을 잡고 0.5초
  sleep 하므로 사이클이 늘어난다). 둘 다 테스트가 지킨다 — 단, 락 쪽 테스트는 **처음에
  가짜였다**(아래 적대적 리뷰 항목 참고). 지금은 `collector._run_cycle()` 을 실제로 실행해
  호출부를 검증하고, 회귀 3종(락 안에 호출 추가·호출을 락 안으로 이동·호출 삭제)에서
  실패하는 것을 확인했다.

  겸사겸사 `notifier` 의 두 결함도 고쳤다: `smtplib.SMTP_SSL` 에 **타임아웃이 없어**
  응답 없는 SMTP 에 호출 스레드가 영구히 멈출 수 있었고(30초 부여), HTML 본문에
  이스케이프가 없어 외부에서 온 종목명이 그대로 들어갔다.
* `[x]` **알림 채널 — Discord Webhook 추가** (2026-08) — 사용자가 Discord 연동 경험이 있어
  그쪽을 1순위로 붙였다. Slack 경로는 지우지 않고 병행 가능하게 남겼다(둘 다 켜도 되고
  이메일과도 병행된다).

  규격은 **Discord 공식 문서 저장소**(`discord/discord-api-docs`)를 클론해 원문으로 확인했다
  — discord.com·discord.dev 도 Slack 과 똑같이 이 환경에서 차단되어 있다.
  · `POST /webhooks/{id}/{token}` — "does not require authentication"(webhook.mdx L204),
    토큰이 URL 경로에 있으므로 Authorization 헤더가 없다.
  · 본문 필드는 `content`(**최대 2000자**), Slack 의 `text` 가 아니다.
  · ★ **`?wait=true` 를 반드시 붙인다.** 공식 문서: `wait=false`(기본값)면
    "a message that is not saved does not return an error". 조용히 버려진 메시지를 성공으로
    읽으면 이 저장소의 "발송 성공 시에만 기준선 이동" 규칙이 그 전환을 **영구 유실**시킨다 —
    적대적 리뷰에서 잡은 4번 결함(MAX_ROWS 꼬리 유실)과 같은 유형이다.
  · ★ **멘션 차단이 Slack 보다 강하다.** `allowed_mentions: {"parse": []}` 는 서버가 파싱
    자체를 하지 않으므로 문자 조합으로 우회할 대상이 없다. 웹훅 기본값은 `{"parse":["users"]}`
    라서 @everyone/@here 는 기본 미파싱이지만 **유저 멘션은 파싱된다** — 종목명에 섞인
    `<@id>` 가 실제로 핑을 보낸다. 공식 문서도 user-generated string 에 이 필드 사용을 권한다.
    Slack 쪽 텍스트 이스케이프는 그대로 유지하되, 두 접근의 강도 차이를 문서에 명시했다.
  · 굵게 문법이 다르다 — Discord `**굵게**`, Slack `*굵게*`. 바꿔 보내면 별표가 글자로
    보이므로 렌더러가 문법을 하드코딩하지 않고 `Dialect` 에서 받아 쓴다.

  구현 중 잡은 것 3개:
  1. **채널 레지스트리를 모듈 상수로 두면 안 된다** — `Channel` 이 import 시점의 함수 객체를
     붙잡아서 나중에 교체해도 **아무 효과가 없으면서 아무도 실패하지 않는다.** 이 저장소가
     반복해서 물린 패턴(규칙 3중 복제·가짜 락 테스트·import 시점 고정 기본값)이라 함수로
     바꾸고 회귀 테스트로 잠갔다.
  2. **길이 상한 안전장치가 계측을 무력화했다** — `render_text` 가 2000자로 잘라 돌려주니
     `fit_rows` 의 `len(...) <= max_chars` 가 **항상 참**이 되어 넘침을 영원히 감지하지 못했다.
     미절단 렌더(`_render_raw`)를 따로 두어 계측과 안전장치를 분리했다. 테스트가 잡았다.
  3. **채널별로 다르게 자르면 안 된다** — Discord 2000자 때문에 행 수를 줄여야 하는데,
     채널마다 다르게 자르면 어느 채널에 무엇이 실렸는지 갈라져 기준선을 어디까지 옮길지
     판단할 수 없다. **가장 빡빡한 채널에 맞춰 한 곳에서만** 자르고, 넘친 만큼은 다음
     사이클로 이월한다.

  실부팅으로 확인: `channels=['discord']` 인식 → 실제 POST 시도 → 프록시 차단 시 로그에
  이유가 남고(`URLError: Tunnel connection failed: 403 Forbidden`) 웹훅 URL·토큰은
  로그 전체에서 0회 등장. 테스트 291 → 330건.
* `[x]` **실사용 1회차에서 드러난 결함 3건** (2026-08)
  사용자가 자기 환경에서 처음 발송을 시도하자 **테스트로는 잡히지 않던 것들**이 나왔다.
  세 건 모두 "코드는 정확히 명세대로 동작하는데 사람이 원인을 찾을 수 없는" 유형이다.

  1. **실패 안내가 존재하지 않는 로그 태그를 가리켰다.** 실패 채널명을 그대로 태그로 써서
     `[email] 경고 확인` 이라 안내했는데, 이메일을 실제로 발송·로깅하는 주체는
     `src/notifier.py` 이고 그 태그는 `[notifier]` 다. `[email]` 은 로그에 **없는** 문자열이라
     사용자가 grep 해도 아무것도 못 찾는다 → `alerts.log_tag()` 로 매핑. 웹훅 채널이
     항등 매핑인 전제(`_post_json` 의 label == 채널명)까지 테스트로 잠갔다.
  2. **`startswith` 만으로는 오염된 URL 을 못 잡는다.** 실제 `.env` 값이 이랬다:
     `DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/https://discordapp.com/api/webhooks/{id}/{token}`
     — README 의 예시 줄(접두사 33자) 뒤에 복사한 URL 을 이어 붙인 형태다. 앞 33자가
     맞으니 검사를 통과해 `/api/alerts/config` 는 `discord` 를 "설정됨"으로 보고했고,
     오류는 한참 뒤 Discord 의 **404 / code 10015 (Unknown webhook)** 로만 드러났다.
     → 구조 검증 추가: `://` 2회 등장·값 내부 공백은 벤더 공통으로 거부하고, Discord 는
     공식 라우트(`POST /webhooks/{webhook.id}/{webhook.token}`, id 는 snowflake =
     숫자)에 맞춰 경로 조각 4개·id 숫자를 확인한다. Slack 은 경로 규격을 1차 출처로
     확인하지 못했으므로 **공통 검사만** 적용한다 — 없는 규격을 지어내면 정상 URL 을
     막는다. README 의 유발 원인(붙여쓰기 가능한 예시 접두사)도 플레이스홀더로 교체.
  3. **원인은 찍히는데 조치를 알려주지 않았다.** `HTTP 404 / {"code":10015}` 만으로는
     다음 수를 알 수 없다 → `_DISCORD_STATUS_HINTS` 로 404·400 에 조치 문구를 붙였다.
     문구는 공식 문서 기준이며, Slack 쪽은 오류 본문 목록을 확인하지 못해 **표를 만들지
     않았다**(그럴듯한 문구는 잘못된 방향으로 디버깅을 보낸다). 이 판단도 테스트로 잠갔다.

  교훈: 채널 규격을 1차 출처로 맞추는 것과, **사용자가 자기 환경에서 원인을 찾을 수 있는
  것**은 별개의 요구사항이다. 후자는 실사용 1회차에서만 드러난다. 테스트 330 → 344건.
* `[x]` **알림 엔진 적대적 리뷰 — 확증된 결함 9건 수정** (2026-08)
  구현 직후 6개 관점(상태기계·동시성·Slack/비밀·영속화·렌더링·통합)으로 병렬 검토하고,
  각 지적을 **반증 시도**에 통과시켰다(18건 제기 → 중복 제거 14건 → 반증 통과 9건).
  전부 **테스트 228건이 통과하는데도 틀렸던** 경로다. CI 초록이 정합성을 보장하지 않는다는
  것을 보여주는 기록이므로 남긴다.

  영향 큰 순:
  1. **출처 센티널 `"store"` 가 전 종목 기준선을 무음 리셋** — `collector._remembered_source()`
     는 신선도 게이트로 fetch 를 건너뛸 때 실제 출처가 아닌 센티널을 돌려준다. 재시작 직후
     첫 사이클은 저장소가 신선하므로 **항상** 저장소 서빙이라, 영속된 `'kis'` 와 달라져
     게이트 ③이 전 종목·전 view_kind 를 재시딩했다. 장중 재기동 한 번에 그 구간의 전환이
     전부 사라진다 — 영속화(게이트 ②)의 존재 이유가 통째로 무효화되는, 유일하게 전역
     규모인 결함이었다. → 알려진 출처만 비교하는 화이트리스트(`_provenance`) + 출처 비교는
     양쪽이 모두 알려진 값일 때만 + `source = COALESCE(...)` 로 저장된 출처 보존.
  2. **재무 '일부' 도착이 그대로 오알림** — `fund_present` 를 `any()` 1비트로 접어서, 세
     비율 중 하나만 도착·소실하는 사이클을 게이트가 감지하지 못했다. `fundamental_score` 는
     세 밴드를 독립 합산하고 `weak_cutoff=1` 이라 관망 구간이 `score==0` **단일 점**이므로,
     `per` 하나만 도착해도 측이 바뀐다(적자·미공시 종목은 `per` 만 결측인 경우가 흔하다).
     → 필드별 존재 **비트마스크**(`fund_mask`, 0~7)로 저장.
     재무 '점수'를 저장하는 대안은 **채택하지 않았다** — KIS 의 PER/PBR 은 현재가로 계산되어
     주가에 따라 움직이므로, 점수를 입력 지문으로 쓰면 진짜 시장 전환까지 무음 흡수해
     오알림을 알림 누락으로 바꾸는 것뿐이다.
  3. **수집 사이클 중 삭제 → 기준선 부활** — 사이클은 시작 시점 목록으로 돌고 **끝**에
     기준선을 쓰므로, 그 사이 삭제가 들어오면 방금 지운 행이 되살아났다(그 행은
     `notified_at=NULL` 이라 쿨다운도 안 걸려 재추가 시 헛알림). → upsert 를
     `INSERT…SELECT … WHERE EXISTS (watchlist … is_active=1)` 로 바꿔 **DB 계층에서 원자적으로**
     차단 + `add_watchlist_item` 양 경로에서 기준선 삭제(경합 없는 지점) + 부팅 시 고아 정리.
  4. **`MAX_ROWS` 로 잘린 꼬리가 영구 유실** — 렌더러가 30건만 실어 보내는데 호출부는 전체
     목록으로 기준선을 옮겨서, 어느 채널에도 실린 적 없는 전환이 '알린 판정'으로 기록되고
     다시는 보고되지 않았다(로그도 건수만 남겨 복원 경로가 없다). 지수 급락일처럼 **알림이
     가장 필요한 날**에만 터진다. → 자르는 주체를 호출부 하나로 모으고, 실린 것만 기준선
     이동. 꼬리는 히스테리시스 카운터에 남아 다음 사이클에 이어서 발송된다(자연 페이지네이션).
  5. **Slack 본문 이스케이프 누락** — 같은 커밋의 이메일 렌더러는 전부 이스케이프하는데
     Slack 경로만 빠져 있었다. 종목명에 `<!channel>` 이 들어가면 알림마다 채널 전원에게
     푸시가 간다. 규격은 Slack 소유 코드로 교차 확인했다: `python-slack-sdk` 의
     `ChannelLink()` → `"<!channel|channel>"`(제어 시퀀스임이 확인됨),
     `java-slack-sdk` `Field.java` L24 는 마크업 필드가 "must be escaped as normal",
     L19 는 마크업 없는 `title` 만 "will be escaped for you" → **이스케이프는 호출자 책임**.
     정확한 엔티티 대응(`& < >` → `&amp; &lt; &gt;`)을 Slack 소유 코드에서 문장으로 찾지는
     못했다(문서 사이트 차단). 이 치환을 택한 이유는 확인된 주입 경로를 확실히 무력화하기
     때문이고, 매핑이 세부에서 다르더라도 실패 모드는 표시상의 문제이지 멘션 주입이 아니다.
  6. **`_state_lock` 불변식을 지키는 테스트가 가짜였다** — `test_dispatch_never_touches_...`
     는 `alerts.process_cycle` 을 직접 불러 **락을 쥔 주체가 애초에 없었고**,
     `acquire(blocking=False)` 가 항상 True 라 위반을 탐지할 수 없었다. 실측 확인: 호출을
     `with _state_lock:` 안으로 옮겨도 228건 전부 통과했다. → `collector._run_cycle()` 을
     실제로 실행해 호출부를 검증하도록 교체(모든 호출의 락 상태를 기록 — 마지막 호출만 보면
     락 밖 호출이 기록을 덮어써 회귀를 놓친다).
  7. **`POST /api/alerts/test` 에 동시성 가드 없음** — 동기 핸들러가 Slack 15초 + SMTP 30초를
     순차 블로킹하며 anyio 스레드풀 토큰을 최대 45초 점유한다. 기본 40토큰이 소진되면 모든
     동기 엔드포인트가 함께 멈춘다. → 세마포어 1개 + 429/`Retry-After`.
     (API 토큰 옵션 자체는 문서화된 설계 선택이므로 이 엔드포인트만 토큰 필수로 바꾸지 않았다.)
  8. **`DECISION_ALERT_VIEWS` 오타·대문자 → 무음 no-op** — 허용값 필터가 잘못된 토큰을 경고
     없이 버려 빈 튜플이 되고, `kinds=()` 인 `diff()` 는 전환도 시딩도 하지 않아 기능 전체가
     죽는데 `enabled` 는 여전히 true 였다. → 관용 파싱(대소문자·공백·중복) + 버린 토큰 경고 +
     빈 결과면 기본값 폴백. `_env_flag` 도 빈 대입이 문서화된 기본값을 뒤집지 않게 고쳤다.
  9. **NaN·inf 가 메시지로 흘러가고, 표 셀이 `lstrip(' ·')` 로 접두어를 되돌림** — `lstrip` 은
     문자 **집합**을 지우는 함수라 접두어 제거 용도로는 위험하다. → 유한값 검사 + 두 형태
     (`_price_text` / `_price_suffix`)를 따로 만들어 되돌릴 필요 자체를 제거.

  반증에서 **탈락한 5건**도 기록해 둔다(수정하지 않음): 확신이 없으면 반증으로 기울이도록
  검증자를 설정했고, 그럴듯하지만 재현되지 않는 지적을 통과시키는 것이 최악이기 때문이다.

  테스트 228 → 284건. 새로 추가한 것 중 `tests/test_alert_state_db.py` 는 알림 관련 SQL 을
  **실제 SQLite 로** 돌리는 첫 테스트다(기존 알림 테스트는 전부 `_FakeDb` 였고, 그래서 위
  3번 결함이 통과했다).
* `[x]` **알림 채널 — Slack Incoming Webhook** (§4 재검토) — §4 는 카카오톡 알림을
  "토큰 갱신(Refresh Token) 프로세스가 까다롭다"는 이유로 접고 Gmail 로 확정했다.
  Slack Incoming Webhook 에는 **그 이유가 존재하지 않는다** — 인증 헤더를 쓰지 않고
  URL 자체가 자격증명이라 갱신 로직이 구조적으로 필요 없다.

  ⚠️ **실발송 미검증.** 이 저장소는 Slack 도메인(`hooks.slack.com`·`api.slack.com`·
  `docs.slack.dev`·`slack.com`)이 이그레스 프록시에서 **전부 403 CONNECT** 로 막힌 환경에서
  개발되었다. 공식 문서 페이지조차 열 수 없었다. 저장소 규칙(추측 금지)에 따라 규격은
  Slack 이 직접 배포하는 **SDK 소스 코드**에서 교차 확인했다:
  · `slackapi/python-slack-sdk` `slack_sdk/webhook/internal_utils.py`
    `_build_request_headers()` — `Content-Type: application/json;charset=utf-8` + UA 만 붙이고
    **Authorization 헤더를 넣지 않는다.** 본문 허용 키: `text`/`blocks`/`attachments`/
    `unfurl_links`/`unfurl_media`/`metadata`.
  · `slackapi/java-slack-sdk` — 성공 판정은 HTTP 200 **AND 본문 문자열 `ok`**(JSON 아님).
    실패는 400/403/404 + `invalid_payload`/`channel_not_found`/`channel_is_archived`,
    유량제한은 429 + `Retry-After`.
  `POST /api/alerts/test` 가 사용자 네트워크에서의 유일한 실검증 경로다.

  구현 선택: `requests` 도 `slack_sdk` 도 쓰지 않고 stdlib `urllib.request` 로 보낸다.
  의존성이 필요 없을 만큼 단순하고(요청 1개·헤더 2개), 무엇보다 `slack_sdk` 는 **DEBUG
  레벨에서 웹훅 URL 전체를 로그에 찍는다** — 그 URL 이 비밀이다. 이 구현은 URL 을 로그·예외
  메시지·API 응답 어디에도 싣지 않고, 그것을 테스트로 잠근다(`test_alert_channels.py`).
* `[x]` **감사 MEDIUM — SQLite lock→429 변환** (2026-08)
  쓰기 경로는 `BEGIN IMMEDIATE` + `busy_timeout=5000` 으로 직렬화하지만, 5초를 넘긴 경합은
  `sqlite3.OperationalError` 가 그대로 라우터 밖으로 나가 **500** 이 됐다. 경합은 일시적이고
  재시도 가능한 상태이므로 429 + `Retry-After` 가 맞다(알림 테스트 발송 동시 제한과 같은 규약).

  락 식별에 **문자열 매칭을 쓰지 않는다** — `OperationalError` 는 `no such table` 등도 함께
  나르고, 메시지로 가르면 SQLite 가 문구를 바꾸는 순간 조용히 오분류된다. 대신
  `sqlite3.Error.sqlite_errorcode` 를 `sqlite3.SQLITE_BUSY`(5)·`SQLITE_LOCKED`(6) 상수와
  비교한다. 확장 결과코드 대비 하위 8비트 마스킹의 근거도 같은 모듈 상수로 확인된다
  (`SQLITE_BUSY_SNAPSHOT` 517 & 0xFF == 5). `sqlite_errorcode` 가 없는 런타임에서는
  변환하지 않는다 — 문구 추측보다 기존 동작 유지가 정직하다.

  검증: 테스트가 예외를 **위조하지 않는다**(위조 객체는 `sqlite_errorcode` 가 비어 분류
  로직을 전혀 검증하지 못한다). 임시 DB에 진짜 락을 걸어 얻은 예외를 쓴다. 실부팅 E2E 로
  외부에서 락을 잡고 `POST /api/watchlist` → 5.026초 후 429 + `retry-after: 2`, 본문 무유출,
  테이블을 지워 만든 non-contention 오류는 그대로 500 + 트레이스백 유지(예외 핸들러가
  `BaseHTTPMiddleware` 아래에서 실제로 걸리는지는 유닛테스트로 증명되지 않는다).
* `[x]` **감사 MEDIUM — 지수 실패 응답이 성공과 같은 수명으로 캐시되던 문제** (2026-08)
  두 지수를 한 리스트로 묶어 캐시하고 **실패 항목도 성공과 같은 TTL(60초)** 로 저장했다.
  두 가지가 잘못됐다:
  1. 순간 장애 하나가 "데이터 없음"을 60초 고정시켰다. 네트워크가 1초 뒤 복구돼도 남은
     59초는 **새로고침해도** 캐시가 실패를 응답했다.
  2. 실패가 직전의 성공 값을 밀어냈다 — 되돌릴 값이 있는데도 버렸다.

  → 캐시를 **지수별**로 쪼개고, 항목마다 "마지막 성공 값(good)"과 "마지막 시도의 실패
  사유(error)"를 따로 들고 있는다. 성공은 `INDICES_CACHE_TTL_SECONDS`, 실패는
  `INDICES_ERROR_RETRY_SECONDS`(기본 10초, 성공 TTL 로 상한 조임). 실패 시 good 이 있고
  `INDICES_STALE_MAX_SECONDS`(기본 900) 안이면 그 값을 `stale=True` +
  `stale_age_seconds` 로 내준다. 그 상한을 둔 이유 — 몇 시간 전 지수를 현재값처럼
  보여주는 것이 "데이터 없음"보다 나쁘다.

  ★ 프론트에도 대응 결함이 있었다. `IndexCard` 가 `item.error` 만 보고 "데이터 없음"을
  띄웠으므로, stale 값을 실어 보내면 **멀쩡한 값을 버렸다.** `item.value` 유무로 갈리도록
  바꾸고 낡음은 별도 배지("· 3분 전")로 표시한다. 서버 계약을 바꿀 때 소비자를 같이
  보지 않으면 개선이 회귀가 된다는 사례로 남긴다.

  라우터에 `Cache-Control: no-store` 를 붙였다 — 실패·stale 을 짧은 수명으로 관리하는 것이
  이 엔드포인트의 계약인데, 클라이언트가 응답을 캐시하면 그 계약이 무의미해진다.

  검증: monotonic 을 손으로 돌려 sleep 없이 TTL 경과를 확인하고, 두 회귀 방향(실패를
  성공 TTL 로 붙잡기 / 실패가 good 을 버리기)을 각각 되돌려 테스트가 실제로 실패하는 것을
  확인했다. 실부팅 E2E: `no-store` 헤더 적용, 재시도 간격 안 `cache_hit=true`,
  11초 뒤 재조회(실패 로그 2→4건).
* `[x]` **감사 LOW — 모의투자 실현손익 구분·체결가 출처 기록** (2026-08)
  두 가지가 기록되지 않아 거래 기록이 사실과 어긋날 수 있었다.

  1. **체결가 출처.** 시세가 없을 때 매도는 장부가(평균단가)로 체결을 허용한다(상폐·거래
     정지로 보유가 영구 고착되는 것을 막기 위한 의도된 설계). 그런데 그 체결이 실제
     시장가 체결과 **구별되지 않게** 기록됐다 — "그 가격에 팔렸다"가 사실이 아닌 기록이
     남고, 체결가 == 평균단가이므로 실현손익이 항상 0이 되어 성적을 왜곡한다.
     → `price_source`('market'|'book')와 `market_status`(체결 시점 장 상태)를 남긴다.
     화면은 "장부가"·"장외" 배지로 표시한다. `price_source` 는 **필수 키워드 인자**다 —
     기본값을 주면 호출부가 잊었을 때 조용히 'market' 으로 기록된다.
  2. **실현손익.** `total_pnl` 은 `total_value - seed` 뿐이어서 "이미 확정된 손익"과
     "아직 팔지 않아 변할 수 있는 손익"이 뭉개졌다. → 매도마다 `realized_pnl`
     = (체결가 − 그 시점 평균단가) × 수량 을 **체결 트랜잭션 안에서** 계산해 남긴다
     (평균단가는 체결 직전에만 정확히 알 수 있고, 나중에 거래내역만으로 되계산하려면
     평균단가 이력이 필요해 불가능하다). 계좌는 누적 실현손익과 미실현손익을 함께 낸다.

  수수료·세금이 없으므로 `realized + unrealized == total_pnl` 이 **정확히** 성립한다.
  유도를 코드 주석에 남기고 항등식을 테스트로 잠갔다.

  마이그레이션은 `decision_alert_state` 와 달리 **DROP 하지 않는다** — 캐시가 아니라
  사용자의 거래 기록이다. `ALTER TABLE ADD COLUMN` 으로 더하고 기존 행은 세 값이 NULL 로
  남는다. NULL 을 'market'/'open'/0 으로 채우지 않는다(모르는 것을 안다고 적는 셈).
  대신 `realized_unknown_trades` 로 그런 매도 건수를 노출해, 분해가 총손익과 맞지 않을 수
  있다는 사실을 화면이 알린다.

  ★ **작업하다 발견: 모의투자 경로에 테스트가 하나도 없었다.** 원자적 체결
  (`BEGIN IMMEDIATE`)·평균이동원가·매도 폴백이 전부 미검증인 상태였다. 새 기능만 덮지 않고
  기존 계약까지 함께 잠갔다(27건). 동시 매수 8스레드로 초과매수·음수잔액이 없음을 실제
  스레드로 확인하고, 회귀 3방향(실현손익을 0으로 / 미실현을 원가 차감 없이 / 마이그레이션이
  DROP)을 되돌려 테스트가 실제로 실패하는 것을 확인했다.

  검증 한계: 이 환경에는 시세가 없어(KIS 자격증명 부재 + Yahoo 403) **API 로 주문을
  체결시켜 보지는 못했다.** 실서버로 확인한 것은 구버전 DB 마이그레이션(기존 거래 1건 보존,
  새 컬럼 NULL, `realized_unknown_trades: 1`)과 시세 부재 시 매수 거부(409)까지다.
  체결 로직 자체는 실제 SQLite 단위테스트로 검증했다.
* `[x]` **배포 하드닝 — 이미지에 UI 포함 · non-root · 의존성 핀** (2026-08)
  "지금 써볼 수 있나?" 라는 물음에 답하려고 실행 경로를 확인하다 **구멍을 찾았다:
  `docker compose up` 을 하면 API 만 뜨고 화면이 없었다.** `.dockerignore` 가 `web/` 을
  통째로 제외했고("handled by Vercel") 서버도 정적 파일을 서빙하지 않았다. Vercel 배포는
  유효한 경로지만, 로컬에서 한 번에 띄워 보려는 사람에게는 쓸 수 없는 이미지였다.

  1. **이미지가 UI 를 함께 빌드한다.** 멀티스테이지 — node 단계에서 `npm ci && npm run
     build`, 산출물만 런타임 단계로 넘긴다(node·node_modules 는 남지 않는다).
  2. **서버가 `web/dist` 를 `/` 에 마운트한다**(`server/static.py`). 두 함정을 각각 잠갔다:
     · SPA 클라이언트 라우팅 — `/paper` 새로고침은 index.html 로 폴백해야 화면이 뜬다.
     · **`/api` 404 를 HTML 로 덮지 않는다.** `/` 마운트는 모든 경로에 걸리므로 라우터에
       없는 `/api/...` 까지 폴백에 걸려 HTML 200 이 나갈 수 있다. 그러면 "그 엔드포인트가
       아직 없다"는 신호가 사라진다 — 실제로 사용자가 옛 커밋으로 서버를 띄웠을 때
       `{"detail":"Not Found"}` 라는 404 **하나로** 원인을 짚었다. 그 진단 능력을 없애면
       안 된다. 마운트가 라우터보다 **뒤에** 등록되는 것도 회귀로 잠갔다(앞이면 API 를 전부
       가린다).
  3. **non-root(uid 10001).** 쓰기가 필요한 경로는 `/app/data` 하나뿐이다(SQLite DB +
     KIS 토큰 캐시 — 둘 다 `dirname(DB_PATH)` 아래). 코드는 root 소유 읽기전용으로 남긴다.
     바인드 마운트한 `./data` 는 호스트 사용자 소유라 compose 가 `user:` 로 실행 uid 를
     맞춘다 — 이걸 안 하면 `unable to open database file` 로 죽는다(chown 을 요구하는
     것보다 uid 를 맞추는 편이 낫다).
  4. **`requirements.lock.txt`.** 느슨한 `requirements.txt` 로 빌드하면 같은 커밋이라도
     상위 버전이 딸려 들어와 어제 되던 이미지가 오늘 깨진다. 락파일은 추측이 아니라
     **테스트 414건이 통과한 환경의 의존성 폐쇄집합**이다(직접 의존 + `uvicorn[standard]`
     extra 를 따라가 계산, 51개).

  검증: **Docker 데몬이 없어 이미지를 빌드하지는 못했다.** 대신 이미지 레이아웃을 그대로
  재현해(코드 `dr-xr-xr-x root` 읽기전용 / `data` 만 uid 10001 소유) uid 10001 로 기동해
  확인했다 — `/` 200 text/html, `/paper` 200(폴백), `/api/nope` 404 JSON,
  `POST /api/watchlist` 201, DB 가 uid 10001 소유로 생성, 권한 오류 0건. 락파일은
  `pip install --dry-run` 으로 51개가 버전 불일치 0·추가 유입 0 으로 재현되는 것을 확인했다.
  `npm ci`·`apt tzdata`·이미지 안 `pip install` 은 미검증으로 남는다(README 에 명시).

* `[x]` **문서에 적은 실행 명령이 깨져 있었다 — 실행 경로를 테스트로 잠금** (2026-08)
  테스트 414건이 전부 통과하는데도 사용자가 README 대로 실행하니 에러가 났다. 단위테스트는
  앱을 **띄우지 않으므로** 그 경로를 검증할 수 없었다. "테스트는 초록인데 못 쓰는
  소프트웨어" — 이 저장소가 반복해서 물린 패턴이다.

  내가 문서에 말없이 넣은 가정 두 개가 원인이었다. 둘 다 **알고 있었는데도** 틀렸다:
  · `uvicorn` 이 PATH 에 있다고 가정 — 사용자는 계속 `python -m uvicorn` 을 쓰고 있었다.
  · 8000 포트가 비어 있다고 가정 — 내가 그 포트에 서버를 띄워 두라고 안내해 놓았다.

  조치:
  1. **tests/test_quickstart_smoke.py** — 서브프로세스로 진짜 서버를 띄우고 HTTP 로 때린다.
     기동·API 응답·`/api` 404·SPA 폴백·포트 충돌 메시지까지 확인한다. 느리지만(≈5초)
     이 경로를 대신 검증할 방법이 없다.
  2. **문서 자체를 테스트한다** — README·DEPLOY 의 코드블록에 맨 `uvicorn` 실행이 있으면
     실패한다. 같은 실수를 두 번 하지 않기 위한 회귀다.
  3. **scripts/run_local.sh** — 실행 전에 위치·포트·파이썬·웹 빌드를 점검하고, 실패하면
     원인과 조치를 한국어로 말해준다. 그 출력 문구를 테스트가 잠근다(이 스크립트의
     산출물은 그 메시지다).
* `[x]` **자격증명 없이 띄운 서버가 1분마다 영원히 경고를 찍던 문제** (2026-08)
  위 스모크 테스트를 만들려고 실서버를 띄워 보다가 발견했다. KIS 자격증명이 없으면
  `kis_ws` 가 백오프 상한(60초)에 걸린 뒤 **끝없이** 재시도하며 경고를 남겼다.
  자격증명은 프로세스 시작 시 `load_dotenv` 로 한 번 읽으므로 실행 중에 생길 수 없다 —
  성공 확률 0의 재시도가 로그를 도배했고, "KIS 없이도 쓸 수 있다"고 안내한 정상 구성이
  고장난 것처럼 보였다.

  → `kis_auth.MissingCredentialsError`(RuntimeError 상속)를 만들어 **문구가 아니라 타입**
  으로 가른다. 메시지 문자열로 분기하면 문구를 다듬는 순간 조용히 오분류된다(SQLite 락
  분류에서 이미 겪은 함정). 이 예외만 재시도를 포기하고, 원인을 설명하는 INFO 한 줄을
  남긴다. 네트워크·rate-limit 실패는 기존 백오프 재연결을 그대로 유지한다(회귀로 잠금).

  실측: 재시작 후 20초 관찰에서 `approval_key` 경고 4건+ → **0건**, INFO 1줄로 대체.

* `[x]` **알림 진단 화면(`/alerts`) + 브라우저 스모크 테스트** (2026-08)
  알림 엔진은 완성됐는데 상태를 볼 방법이 `.env` 편집과 curl 뿐이었다. 실제로 값 오염
  (웹훅 접두사 중복)을 찾는 데 여러 왕복이 걸렸고, 그 과정 전부가 화면 하나로 대체된다.

  읽기 전용이다 — 웹훅 URL·앱 비밀번호는 그 자체가 비밀이라 화면에서 다루지 않는다.
  답하는 질문은 셋: 어떤 채널이 **인식됐나**(오염된 값은 거부되어 비어 보인다) /
  지금 발송 조건인가 / 무엇을 마지막으로 알렸나(기준선). 테스트 발송 버튼이 채널별
  성공·실패와 **실패 시 볼 로그 태그**를 함께 보여준다.

  ★ **프런트엔드에 실행 검증이 없다는 것을 이 작업에서 확인했다.** CI 는 `tsc -b` 와
  `vite build` 만 돌린다 — 둘 다 통과해도 화면이 비거나 레이아웃이 깨질 수 있다(빈 화면은
  컴파일 에러가 아니다). 백엔드에는 실제 서버를 띄우는 스모크 테스트가 있는데 프런트엔드에는
  대응물이 없었다. → `scripts/smoke_ui.mjs` — 실제 Chromium 으로 4개 화면을 열어
  마운트·문구·탭바 줄수·콘솔 에러·테스트 발송 왕복을 확인한다(14건).

  그리고 그 스모크 테스트가 **바로 결함을 하나 잡았다**: `.tabbar` 가
  `grid-template-columns: repeat(3, 1fr)` 로 탭 개수를 하드코딩해서, 4번째 탭을 넣자
  탭바가 2줄로 깨졌다. tsc·DOM 검사·pytest 는 전부 통과했고 **스크린샷에서만** 보였다.
  `grid-auto-flow: column` 으로 고쳐 탭이 늘어도 한 줄을 유지하게 하고, 줄 수 검사를
  스모크에 추가해 회귀로 잠갔다(3열로 되돌려 실패 확인).

  CSS 변수 오타도 같은 방식으로 잡았다 — 존재하지 않는 `--surface-variant` 를 썼는데
  tsc 는 CSS 를 보지 않는다. 실재하는 `--surface-container` 로 교체(라이트·다크 모두 정의됨).

  검증: ruff·tsc·pytest 440건·스모크 14건 통과. 라이트/다크 × 3화면 스크린샷으로 육안 확인.
  CI 에 브라우저 단계는 넣지 않았다(무거운 의존성) — 손으로 돌리는 도구로 문서화했다.

* `[x]` **알림 이력(전환 기록) + 화면** (2026-08)
  `decision_alert_state` 는 종목·종류당 **한 행**(마지막으로 알린 판정)만 들고 게이트 판단에
  쓰이는 캐시라, "이 종목 판정이 언제 어떻게 바뀌었나"에 답할 수 없었다. 알림을 놓쳤거나
  지난 며칠을 되짚고 싶을 때 복원할 경로가 없었다.
  → `decision_alert_history` (append-only) + `GET /api/alerts/history` + `/alerts` 화면의
  "최근 알림" 섹션.

  **무엇을 남기는지의 정의가 이 기능의 핵심이다.** 발송에 성공한 것만, 성공한 채널만
  남긴다 — 기준선 이동과 정확히 같은 조건이다. 이유:
  · 쿨다운·히스테리시스로 눌린 전환은 `_pending` 에 남아 조건이 풀리면 발화한다 —
    유실이 아니라 지연이다. 이력에 넣으면 같은 전환이 두 번 보이고 "안 왔다"와
    "아직 안 왔다"를 구분할 수 없다.
  · 실패한 채널을 실으면 "어디로 갔는지"가 사실과 달라진다.
  · 테스트 발송(`POST /api/alerts/test`)은 가짜 전환이므로 남지 않는다 — 그래서 기록은
    `dispatch` 가 아니라 `process_cycle` 에서 한다.
  보존 상한 500행(개인용 앱이라 무한 성장은 DB 만 키운다). 이력 쓰기가 실패해도 발송·기준선은
  이미 끝났으므로 사이클을 멈추지 않고 경고만 남긴다.

  ★ 스모크 테스트가 이번에도 잡았다 — 그리고 **내가 눈으로 헛짚은 것도 교정했다**:
  fullPage 스크린샷은 `position: fixed` 탭바를 뷰포트 위치에 그려서 푸터가 가려진 것처럼
  보인다. 실제로 재보니 겹침은 없었고(여유 3px), 대신 진짜 문제가 드러났다 —
  `.tab-layout .app` 의 하단 여백이 72px 인데 탭바가 68px 이라 **여유가 4px** 였다.
  섹션 하나만 더 붙으면 가려지는 값이다. 88px 로 올리고(여유 19px), 실제 겹침을 재는
  검사를 스모크에 추가해 잠갔다(72px→0 으로 되돌려 `69px 겹침` 검출 확인).

  스크립트 자체의 레이스도 하나 고쳤다: config 응답이 도착하기 전에 버튼 상태를 읽어
  "채널 미설정"으로 오판했다. 로딩 완료를 기다리게 바꾸고 3회 연속 통과를 확인했다.

  검증: ruff·tsc·pytest 453건·스모크 19건(3회 연속) 통과. 회귀 3방향(실패 채널 기록 /
  발송 실패에도 기록 / 보존 상한 제거)을 되돌려 각각 실패 확인. 실서버로 이력 엔드포인트와
  화면을 확인한 뒤 종료.

## 21. 판정 엔진 자체의 한계 (2026-07 감사 추가 · 일부 해소)

> §15.1 은 판정 로직을 "상대적으로 강한 것"으로 분류했으나, 코드를 다시 보면 **엔진 자체가 얇다**.
> 백테스트로 검증할 대상의 품질 문제이므로 §15.2 시그니처 기능보다 상위 리스크로 기록한다.

* `[x]` **규칙 3중 복제 제거** — 같은 판정 규칙이 `src/indicators.py`·`server/services/backtest.py`·`web/src/utils/factorScoring.ts` 세 곳에 손으로 복제되어 있었고, 어긋나도 아무도 실패하지 않았다(점수표 lookup 이 `.get(label, 0)` 이라 라벨 하나만 달라져도 조용히 전부 0점=관망; TS 쪽은 라벨 타입이 `string | null` 이라 컴파일 에러도 없음). → `src/decision_rules.py` 로 단일화, `factorScoring.ts` 삭제.
* `[x]` **재무 단독 '강력' 등급 결함 수정** — 재무 점수 최대치(±3)가 장기 임계값(3)과 같아 기술 신호 없이 `PER<10 + PBR<1 + ROE≥15` 만으로 강력매수가 나왔다. 기술 지표가 계산 불가여도 0점=중립으로 취급되므로 **입력 절반이 결측인 상태에서 최고 확신 등급**이 나오는 구조였다. → 강력 등급에 기술 점수의 같은 방향 확증을 요구(`long_strong_requires_tech_confirm`). 49칸 진리표 중 2칸만 변경, 매수/매도 대칭 유지, 백테스트 수치 불변(재무를 항상 None 으로 넘기므로).
* `[x]` **판정 근거 화면-백엔드 불일치 제거** — 프론트가 점수를 재계산했고 `factorScoring.ts` 주석이 "백엔드 점수와 불일치할 수 있으므로 화면은 프론트 합계를 우선한다"고 명시하고 있었다. 게다가 규칙 설명 캡션이 규칙을 **잘못** 말했다("합계 +3 이상이면 매수" → 실제는 +1 매수 / +3 강력매수). → 스냅샷 응답에 `factors.breakdown_short/long` + 응답 레벨 `rules` 를 실어 프론트를 렌더 전용으로 전환.
* `[x]` **단기 판정의 데이터 취약성 완화** — 60분봉이 yfinance 전용이라 야후 지연·결측·429 시 단기 판정이 통째로 사라졌다. → KIS 분봉(FHKST03010230, 공식 예제 3곳 교차확인) 1차 → yfinance 폴백 경로 구현. **기본 비활성**(`KIS_MINUTE_ENABLED`): 종목당 초기 백필 24~32회 호출이라 유량제한 위험이 있고, 자격증명이 없어 실제 응답으로 검증하지 못했다. 파서·리샘플러는 공식 필드명 픽스처로 단위테스트했다.
* `[~]` **가중치·임계값이 미검증 상수** — 값 자체는 **바꾸지 않았다.** 근거 없이 미검증 상수를 다른 미검증 상수로 바꾸는 것이기 때문이다. 대신 되먹임 경로를 만들었다: `server/services/rule_eval.py` + `scripts/evaluate_rules.py`. 자세한 내용은 §22.
* `[ ]` **지표 2개 의존** — 판정은 여전히 MACD 라벨 + RSI 라벨뿐이다. 거래량·추세강도(ADX)·변동성(ATR)·시장 레짐 미반영. 팩터를 추가하려면 가중치를 정해야 하는데 그건 근거가 필요하다(§22 의 검출력 문제).
* `[ ]` **계산은 하고 판정엔 안 쓰는 지표** — 볼린저밴드는 `collector` 에서 산출·화면 표시되지만 점수에 들어가지 않는다. 위와 같은 이유로 보류.
* `[ ]` **실시간 틱은 판정에 미반영** — WS 틱은 표시 전용이고, 판정은 신선도 게이트(일봉 600초·60분봉 5분) 기반 저장소 값으로 계산된다.

## 22. 룰 평가 하네스 — 그리고 "지금은 검증할 수 없다"는 결론

`server/services/rule_eval.py` + `scripts/evaluate_rules.py`. 목적은 가중치를 최적화하는 것이 **아니라**,
"바꿀 근거가 있는가"에 정직하게 답하는 것이다.

### 22.1 담은 것
| 출력 | 왜 |
|---|---|
| **검출력(MDE)** — 가장 먼저 보는 값 | 이 패널로 몇 pp 차이를 구분할 수 있는지. 기대효과보다 크면 실험 자체가 무의미하다 |
| **base rate** — 무조건부 P(선행수익>0) | 상승 구간에서 base rate 60%면 "매수 적중률 60%"는 정보량 0. 적중률은 lift 로만 읽는다 |
| **단조성** — 점수 버킷별 평균 수익 + Spearman ρ | 가중치가 의미 있다면 점수가 높을수록 수익이 높아야 한다. **단일 사전지정 검정**이라 다중검정 함정이 없다 |
| **비교군** — `always_buy_ish` / `always_watch_ish` | 기준선을 이겼지만 단순 보유에 지는 변형은 개선이 아니라 상승장 재발견이다 |
| **짝지은 비교 + 불일치쌍 수** | 두 룰이 갈린 봉만 비교. 불일치쌍이 적으면 어떤 차이도 결론이 아니다 |
| **클러스터 표본** | 같은 날 여러 종목이 동시에 신호를 내면 독립 관측 N건이 아니라 1건이다. 종목별 겹침 보정만 하고 합치면 표본이 종목 수만큼 부푼다 |

### 22.2 일부러 넣지 않은 것 (그리고 그 이유)
실험 원장(사전등록·누적 K)·holdout 예산·블록 부트스트랩·**best-of-K 귀무분포**·BH q-value·walk-forward 폴드.

이것들은 전부 "여러 변형 중 승자를 고를 때" 과최적화를 막는 장치다. 그런데 위 MDE 계산이
**"지금 데이터로는 승자를 고를 수 없다"**고 답한다 — 400봉·horizon 20 이면 클러스터 표본이
수십 건이라 검출한계가 30pp 를 넘고, 가중치 ±1 조정의 기대효과는 1~3pp 다. 쓸 일이 없는
기계를 먼저 만드는 것은 과설계다.

**실제로 채택 결정을 시도할 만큼 이력이 쌓이면(수년치 × 수십 종목) 위 목록을 먼저 추가해야 한다.**
지금 리포트는 변형 비교 구간에 "K개를 동시에 비교했으므로 최고를 고르면 그것만으로 과최적화"라는
경고를 기계적으로 출력한다.

### 22.3 검증 불가 항목
* **재무 가중치는 이 하네스로 검증할 수 없다.** 과거 PER/PBR/ROE 이력이 없어 재무 점수가 항상 0이다.
  재무 변형은 결과가 기준선과 **완전히 동일**하게 나오는데, 이걸 "차이 없음 = 현행 가중치 문제없음"
  으로 읽으면 명백한 거짓이다 → `fundamentals_testable: False` 로 명시한다.
* 이 환경(프록시가 Yahoo 403 차단 + KIS 자격증명 부재)에서는 **실제 시장 데이터로 하네스를 돌릴 수 없었다.**
  합성 일봉 3종목 × 400봉으로 CLI 를 구동해 파이프라인과 게이트 동작만 확인했고, 그때도 판정은
  `UNDERPOWERED` 였다.

> **E2E 검증 한계:** 프록시가 Yahoo(403) 차단 + KIS 자격증명 부재 → 지수는 failsafe로 200(에러 필드 포함) 응답 확인. 백테스트·DCA·모의투자는 시드 DB로 검증 완료.
> 2026-07 작업분도 동일 제약 하에 검증했다: 시트 동기화는 gspread 호출을 monkeypatch 한 단위테스트 + 자격증명 부재 시 `enabled:false` 응답 확인(실제 Google Sheets 왕복은 미검증), WS 워치독은 가짜 연결로 무수신·PINGPONG·파싱이상 경로 검증(실 KIS 세션 미검증).